# 补录与预览周视图修复记录

- 基线：`5befe6a3`，独占 warm `general-4`，`REUSE_ONLY`，未安装依赖。此前上传放行的 `.195@d4f0cbe3` 不含本轮修改；本轮未分配新版号、未上传、未操作生产。
- 引入点：`git log -S 'calendarShiftBadge'` 与 `git blame` 定位徽标首汉字逻辑到 `8e68a480`；预览单独周格及手排偏好映射由 `672d52ef` 引入。补录页沿用旧月历，未读取群组视图偏好。两张用户截图是现象证据，未提供当前构建身份，不能作为本轮原生验收。
- 修复：共用徽标改用首页的 `truncateCalendarBadgeLabel`，徽标宽度容纳两个字符；补录读取群组默认视图，列表映射月，跨月周使用已读取的相邻月排班，并避免同月翻周重读闪烁。手排预览的周视图交由已有 `calendar-month` 分页/单元格组件呈现，复用滑动、定位动效、动态高度、日期详情、灰色已有排班、节假日与法定补班日标识；移除预览专属周格 WXML/WXSS。发布预览外层仍以 `65vh` 限高并在窗口内滚动。
- 定向用例钉住 N/NP/A/D/电脑原样徽标、跨月周与灰色已有班次、密集周高度、相邻月补录及同月翻周不重读；旧单字符和旧周视图调用断言按新需求更新。定向 42/42、根 `pnpm verify` 1306/451 跳过、Mini production verify、TypeScript 与最终 WXML 编译通过；包体由 4,797,548 增至 4,801,743 B（+4,195 B），主包 1,826,112 B。主包超过 1.5 MB 和矩阵 1510 节点为既有警告。模拟器可打开补录路由但停在“正在加载既往排班”，没有真实数据的可视周历证据。小米 14 同构建体验版仍待用户验证。
- 待验：护士群补录默认周、月/列表映射、跨月排班与补录；手排生成/草稿/已发布预览的周滑动、定位、密集单元格、详情、灰色对照和节假日；320px 与小米 14 原生布局。
