# Feedback7：群组管理与手动排班交互

## 范围与已确认选择

- 2026-09-09 用户提供17项要求及10张截图；本轮仅修改小程序与跟踪文档。
- 用户重新确认：每次进入手动排班默认新建空白模板，显式选择旧模板才载入已保存的班种。
- 岗位成员排序箭头：已查明上轮移除了排序处理器但遗留WXML；用户当轮确认删除失效排序箭头，保留成员勾选与保存。
- 不安装依赖、不操作微信开发者工具、不上传体验版、不访问生产。

## 问题与实现

| 编号 | 优先级 | 用户问题／根因 | 实现与验证 |
| --- | --- | --- | --- |
| F7-01 | P2 | 群组操作使用常驻managementInfo；与已存在日历偏好toast通路分离 | 第1项：复用ui-toast、showCalendarFeedback及唯一两秒计时器；成功与失败结果瞬时显示 |
| F7-02 | P2 | 成员表单在scroll-view流内，展开时落到页面下方 | 第2项：表单移至页面滚动区外的ui-sheet，保留保存、取消与原有写入协议 |
| F7-03 | P2 | 按钮width:100%、44px最小高及48%操作区造成留白 | 第3–6项：统一8px内边距、内容宽度、成员操作右对齐，解散独立置于页尾；同步大字体规则 |
| F7-04 | P2 | 页面自动openTemplate(templates[0])；原生picker与额外日期框高度不一致 | 第7–8项：默认空白；复用ui-selector并增可选多选模式、ui-date-picker与UiWheelColumn；五框58px，岗位40%／日期60% |
| F7-05 | P2 | 预览仅统计，风险勾选始终渲染；阶段标识不可点击 | 第9–12项：月历浏览、连续值班>0才显示强制勾选；交接轨进入编辑、预览、草稿、发布记录独立内嵌状态 |
| F7-06 | P2 | 草稿无预览入口，发布查看仅文字摘要，按钮留白过大 | 第13–15项：草稿与记录弹窗共用schedule-calendar-preview，组合既有calendar-month与翻月状态机；草稿跨月合并读取，按钮及徽标统一内边距 |
| F7-07 | P2 | 44px input仍有上下10px padding；拟补录色为亮红 | 第16项：去绿色提示，input改水平padding及44px行高；保留已有removed/added模型，新增人名暗红；未重写补录逻辑 |
| F7-08 | P2 | 旧排序箭头WXML未随处理器删除；岗位标题继承column | 第17项：标题与删除按钮横排右对齐；按用户确认删除失效箭头，保留勾选与保存 |

以上均为源码及Node证据支持的局部问题，置信度高、可回滚；光标原生位置、滚轮手感和最终视觉仍需同构建小米14验证。

## 引入点与行为审查

- 手排自动选旧模板、点格标题、原模板控件：`591ccff6`；已运行git log -S及git blame。
- 群组整行按钮/44px：`0d971de1`；内联表单、常驻管理通知、48%操作区：`70f9a98f`。
- 大字体满宽与52px：`1725ee7a`；成员卡复用：`6d0575d0`；操作区padding：`831160d5`；现有toast计时：`d3d6e2c6`。
- 岗位标题column：`38233039`；排序处理器/样式被删但WXML残留：`057af270`。
- 补录输入从textarea改input且保留垂直padding、removed/added模型：`528722f4`。
- 本轮为明确行为调整，不宣称整体语义等价。周期和成员事件转发保留实例接收者、显式保存、清除被移除人员/超出周期格的行为；新增选项合法性检查。
- 预览不发布、不写排班；跨月草稿预览只调用既有只读接口。原发布、删除、撤销的权限、版本、幂等键和确认门禁保留。
- 多选模式仅显式multiple时生效；其他单选调用保持索引事件协议。
- 通知复用已有计时器，不新增第二套toast、滚轮引擎或月历手势。

## 验证记录

- 基线：`a9852f67`，独占`runtime/wt/general-1`；源码干净，根目录原有未跟踪内容未动。
- 路由：L1／REUSE_ONLY；Acquire、带租约ReuseOnly、mini Bootstrap成功；三个共享生产者复用，无安装。
- 基线命令：`pnpm icon:parity`通过；手排/发布4文件15测试通过；补录/配置4文件20测试通过；thin-page 2测试通过。
- 基线`pnpm --filter @schedule/miniprogram verify`通过：主包1749533、总包5118172字节，Worklet 2/2。原1.5M主包和矩阵节点预警保留；独立构建耗时未单独计量，不作性能改善比较。
- 新增回归先红：feedback7-manual最初3项失败，feedback7-group 2项失败，feedback7-backfill 2项失败；实现后通过。
- 完整小程序回归首轮：979通过、15跳过、1失败；失败是旧ui-toast清单禁止手排toast，与新需求冲突。更新对应清单后通知/手排/组织44项定向复测通过。
- 布局：`node apps/miniprogram/scripts/feedback7-layout.mjs`，320×844及390×844检查五框58px、岗位/日期同排、按钮四周8px、成员行右对齐及大字体自然换行；桌面CSS模型，非微信运行时。
- 最终Mini verify通过（7.36秒）：主包1751925、总包5132987字节；分包scheduling422151、organization1016258、workflows796663、insights1059924、diagnostics86066字节。最大文件pages/workbench/index.js219674字节；手排index.js187534字节。
- 原生Console/Network、冷启动、光标、滚轮与全平台兼容：当前工具无法测量，暂未验证。截图未携带完整版本/renderer/基础库元数据，只用于定位反馈。

- 最终完整小程序回归：154文件通过、1文件跳过；981测试通过、15跳过，113.55秒。已更新的旧断言通过。
- 最终format:check、修改范围ESLint、smoke:check-core、根连续性/发现策略6测试、确定性与icon parity通过。未触及Web核心，无需pnpm smoke:browser。
- 群组通知新增“离开/回来后旧结果不重现”回归先失败后通过，并验证错误通知1999ms保留、2000ms消失；读后写反馈携带原群组/加载代次。
- 证据位于ignored `runtime/wt/general-1/runtime/audit/feedback7/`，不提交截图或日志。检查点消息：`fix(miniprogram): refine group and manual scheduling workflows`。

## 下一步与停止条件

完成Git检查点后停止；体验版上传需用户对精确检查点当次批准。当前不创建生产备份或部署，不分配体验版版本。
