# 日期级发布与成员绑定修复交付记录（2026-09-24）

## 结果

提交 `162ef4c1` 已推送；生产 API、Web 与迁移部署为同一提交，schema 为 64。小程序不可变体验版 `0.1.0-p10.20260924.192` 已上传并只追加放行。未提交审核或正式发布。

发布冲突现在按同岗位的实际重复日期预检；无重复时增量发布，有重复时整日替换并保留其他日期的原班次 ID 与工作流。每次合并前保存旧月完整快照。绑定预览对成员码展示完整姓名/工号/有效时间，单次确认直接绑定；取消或失败可进入访客码入口。成员码有效期 24 小时，管理员绑定链接仍为 10 分钟，旧预览兼容。二维码保留扫码边距并缩短底部留白。

## 验证层级

- 静态与 Node：完整 `pnpm verify` 通过（Mini 1257/16 跳过、根 1306/450 跳过）；Mini production verify 总包 4801680 B、主包 1824570 B；CI dry-run 与 trial-lineage 通过。
- 真实本地 MySQL：手排 35/35、发布 3/3、仓库合并/工作流/专项恢复 10/10、绑定 8/8、迁移 28/28。浏览器 smoke 在本地 API/Web 通过，`smoke:check-core` 通过。
- 生产：备份 `9c86d238-cdb6-438c-bb7a-37549106b6e5`（126312704 B）的记录、文件大小和 SHA-256 一致；`162ef4c1`/schema 64 部署与完整 verifier 通过。恢复前精确版本/日期/工作流只读守卫通过；恢复后当前 2026-12 修订号 6、1–31 日 31 个有效班次，31 日旧班次 ID 保留，修订 5 保存为完整快照。实际日历读数与 CSV 导出均为 31 条且覆盖每一天，统计快照 plannedCount=31，补偿通知 0。恢复后 verifier 再次通过。
- 上传：`.192` 的 production receipt、Manifest `5c94240f…beefa`、远端不可变 tag 与提交相符；`.192` 和 `.191` 公网能力响应 200，未知版本 426；独立 allowlist/ECS verifier 通过。
- 开发者工具：可打开访客页；截图接口两次返回 `waitForAutomatorReady timeout`。旧截图仅 35×75，不能用于 390×844 或 320px 视觉对照。当前工具无法测量这两种尺寸，暂未验证。小米 14 原生体验版同样待用户提供证据，不以模拟器或上传证明真机通过。

## 风险与下一步

迁移 64 将软删除班次从唯一槽位约束中排除。旧 release 的 schema 63 兼容范围不覆盖 64，普通应用回滚控制会拒绝；需要回退时先审查数据库恢复或前向修复路径。备份和旧发布快照均已保留。

下一步只做 `.192@162ef4c1` 的小米 14 原生复核：日期级发布/覆盖、两个预览的定位动画、一次绑定与访客入口、二维码版面和 24 小时过期边界。取得同构建证据前保持“待用户复核”。
