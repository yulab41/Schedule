# feedback8：真机反馈修复与账户清理调查

## 范围和确认

- 基线：main/origin main `6729ec0a`，上一交付应用`50744302`／体验版98。独占`runtime/wt/general-1`，REUSE_ONLY，未安装依赖。
- 用户要求9项：解散按钮、周期弹窗、预览月历、草稿错误、发布月历弹窗、补录、邀请卡片、删除待设置账户、微信通知。
- 已确认：重叠日期原排班黑字删除线，下方拟排班暗红字；邀请使用微信好友/群聊小程序卡片。账户原全删方案已取消，最终只删除重复姓名或从未历史排班的3个待设置账户，具体影响另行确认后已执行。
- 账户清理已完成，见feedback8-account-cleanup.md；唯一班种自动选中属于另行弹窗的可选改进，未获确认前保留原来的手动选择。

## 已实现，待最终运行检查与真机复核

1. 解散按钮复用退出登录的视觉参数，独立居中。周期弹窗复用UiSheet新增content尺寸，滚轮不变，确认按钮只显示“确定”。
2. 月历复用calendar-month/calendar-cell，保留班种颜色及单个中文标识；窄弹窗用紧凑姓名布局。月历高度变化传至父弹窗，超出可用屏幕时仍可滚动。
3. 手排预览读取当前岗位的已有排班，按日期/格位合并原排班删除线与拟排班暗红字；没有本次拟排班的日期灰显且不可选中。浏览月份补读已有排班，异步数据到达不重置正在查看的月份。
4. 补全存储草稿预览的严格schema，使用既有生成器更新compact schema；DELETE明确发送JSON空对象，保留UUID幂等头和空成功响应解码。
5. 补录的未选班种使用白底有色边框，显示精准缺项提示；复用瞬时通知及既有计时器。完整WXML绑定和嵌套select事件链测试覆盖原姓名删除线、新姓名暗红字及空格新增。
6. 邀请采用原生share按钮及Page分享回调；已有`pages/invite/invite`地址作为轻量入口，接收逻辑放组织分包。登录后返回邀请，姓名确认后接受，账号合并后先读取新token对应资料再保存会话；不自动接受或转发。
7. 邀请异步状态保护覆盖旧401不清除新登录、接受成功后仅重试资料/导航、失效身份重新登录、卸载后晚到结果丢弃、过期卡片不继续分享。令牌不写storage或日志。
8. 微信当前平台模板只读核验为四字段：thing6、character_string7、thing9、thing8；发送映射已改为换班状态、班次日期、姓名、班种，普通提醒从真实排班关联获取数据，本人测试使用明确测试内容。
9. 通知发送前校验当前收件人、有效群组/成员、已发布未来排班；换班标记复用月历事件归并。提醒批次包含成员，兼容旧通知去重。模板内容不合法标记失败，已失效排班跳过；诊断只有实际查询当前模板并匹配字段后才显示已核验。

## 原因和引入点

| 问题 | 来源 |
|---|---|
| 周期sheet沿用固定78vh | UiSheet默认`304d742f`；手排使用`50744302` |
| 预览丢颜色、固定390px外层、补录提示被删除 | `50744302` |
| 草稿schema只声明两个字段却strict | `.strict()`改动`d7793719`，Mini decoder接入`591ccff6` |
| DELETE没有body | `591ccff6`，transport抽取`9e3a966c`保留；Fastify本地复现空JSON400，未取得真机请求头 |
| 邀请路径未注册／仅显示文本 | 原路径`a50c4fce`、当前返回`cf453205`、Mini文本`ddd5c107` |
| 固定thing1/thing2 | `ef3d20ca`；实际平台模板不匹配导致截图47003 |
| 提醒批次未包含成员 | `52e9e1f4`；换班后旧批次阻挡新成员提醒 |

已对回归点执行log -S/blame；本轮新代码复核发现的邀请状态问题在未提交阶段先以失败测试复现，再修复。

## 生产只读事实与删除边界

- 初始只读诊断时服务器live读得`36fae3d145982d793c1dee243a6eb12867962fb6`；后来获具体删除授权，已备份并清理3账户，应用live不变。未部署本轮应用、改允许列表或发送消息。
- 35账户中24已设密码、11未设密码；后者没有已识别管理员或微信身份，但关联10个成员记录（1个有效）、1002条历史排班，全部past且不含今天或未来排班。
- 还有58处事件操作者引用、58处发起人引用、7处补录操作者引用，及请假/换班/加扣班审批、15条通知、16条幂等记录等。外键阻止直接删除；不能关闭外键强删或把保留姓名快照说成完整保留人员统计。
- 用户看不到弹窗后改用直接编号回复，取消全删11账户，选择只筛选重复姓名或从未出现历史排班的待设置账户。展示3个账户的必要姓名/短标识及影响，再获“确认删除”，已完成。跟踪文档只记录脱敏数量和校验事实，不保存姓名、手机号、身份凭据或业务记录。

## 验证记录

- 基线：feedback7定向9项通过；最初误用node --test加载Vitest，修正执行器后通过，非产品失败。
- 先红后绿：完整草稿decoder和DELETE descriptor两项；预览颜色/单字标识；邀请旧401、导航重试、资料401三项；分享卸载与过期两项；通知解散、换班后新成员、字段无效分类三项。
- Fastify注入：空JSON DELETE400，端点的`{}`请求204，幂等头保留且空成功响应可解码。
- 真实本地MySQL：通知与本人诊断10项通过；补入通知边界后15项（含3项字段unit）通过，全部使用模拟微信网关。canonical旧.env测试凭据失配后，复用既有仅本地容器配置封装；没有改数据库凭据或绕过测试。
- Mini完整首轮：985通过、15跳过、6个旧页面清单断言失败；新增分包路由补入清单后相关33项通过。最终完整结果待记录。
- Mini verify：邀请接收页最初放主包导致1915642字节超1.8M；改用轻量入口＋组织分包后通过，主包1763959、总包5321289字节，保留已有1.5M及矩阵节点预警。该数值为中间检查，最终值以后续完整检查为准。
- CSS模型：320/390宽度，周期sheet349px；五/六行月历弹窗相差54px，姓名无裁剪、无横向溢出；人工查看生成截图确认。仅桌面CSS几何，不能代替Skyline或小米14。
- 运行/浏览器验证：pnpm smoke:browser，通过既有本地配置封装执行。管理员、成员、访客vkey及访问记录全流程通过，无浏览器错误。首次启动缺VITE_AUTH_DEV_MODE导致fixture入口缺失；启用明确本地模式后复测通过。合成管理员标志已恢复。
- 当前工具无法测量本轮微信原生触摸、分享交付、真实收信、冷启动和跨平台表现；未声称真机验收。

## 行为审计和下一步

新增或改变行为均限于上述用户请求及所需的接口/会话正确性；不提审或正式发布，不重发历史失败通知。collectMarkers只增加导出，原月历调用、接收者、顺序和空值语义不变。

完成最终本地检查后形成应用修复检查点；账户清理已完成，不可重复执行。原最终verify中断，无最终结果；本地Docker本轮启动失败，待恢复后继续必要的本地数据库检查。新体验版上传、生产应用部署及真实收信验证尚未完成。
