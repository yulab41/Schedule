# UX-CLEANUP-10 截图反馈2

基于已发布的 `.91@831160d5`，沿用 RUN_ID `ux-cleanup-10-20260906083903`。本轮只实施用户新增的 8 项反馈，不自动部署或上传。

## 已实现

- 群内公开手机号：保存中继续使用开关 loading 状态，但不再在标题下插入“正在保存”文字；标题字号收紧到 14px。
- 群组管理：退出群组按钮移动到联系方式公开区域之后、页面内容底部；原确认、权限和提交保护保留。
- “我的”：完整统计继续跳转事件与统计页，目标页首次打开排班统计；全天班标识改为 28px 紧凑胶囊并收小垂直内边距；修改登录密码胶囊文字为 12px。
- 首页筛选：保留 canonical SVG 资源供生成审计，但实际 Skyline 画面使用三条原生 `view` 横线，避开三张 SVG 图片叠层导致的跨端视觉歧义；三段原有动画选择器继续生效。
- 换班、请假、加扣班：原生确认框 `fail` 和用户取消分开处理。确认框异常时显示可重试错误，不提交请求；用户确认仍携带原成员/版本/幂等参数，取消保持安静。

## 根因与证据

- 工作流根因来自三个 controller 的 `showModal.fail → resolve(false)`：底层异常被当成用户取消，因此界面无反馈。修复提交 `ccdcd8fff20050293b7f280206c331948a1ca208`，保留生命周期 token、权限检查、expectedVersion 和 operationId。
- 先前 `.89/.90` 三个 SVG 路径本身没有箭头；反馈2证据确认 Mini 使用三张独立 SVG image，Web 使用单个内联 SVG。筛选实际改为原生线条后，Node 布局测试验证宽度 16/12/8、统一2px高度和递进左边距；小米14仍待验证。
- 修改来源已用 `git log -S`/`git blame` 核对：统计默认来自 `ee6f9cb8`，个人页旧间距来自 `a50b423b`/`6d0575d0`，工作流确认来自 `bc32a4f1`。

## 验证与发布边界

- `pnpm --filter @schedule/miniprogram exec vitest run ...` 工作流/布局/统计定向：46项通过；Mini完整测试：139文件、853通过、13条件skip；Mini `verify` 通过；`typecheck`、`icon:parity:check`、format、lint、`git diff --check` 通过。
- 本轮没有生产连接、数据库写入、体验版上传或正式发布；最新已交付仍为 `.91@831160d5`。新代码需要用户另行明确授权后才能发布到 API/Web 或上传新体验版。
- 真机、微信原生确认框、真实管理员驳回、请假和加扣班业务操作尚未验证；使用隔离 mock 验证，没有拨打电话、修改隐私、提交真实工作流或退出真实群组。
