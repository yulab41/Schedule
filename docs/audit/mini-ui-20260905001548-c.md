# Mini UI 20260905001548 — C

范围：需求 4 的事件记录调用侧、需求 7 颜色选择器、需求 8 月/周 today 标记。
用户已确认设计并授权本地提交；本任务不 push、不分配版本、不上传、不修改总控状态。

- Branch：`codex/mini-ui-20260905001548-c`。
- Base：`32792fe47b1c3769bebe14a0632e3de9279e7b32`；起始 tracked/untracked 均干净。
- Worktree：canonical `runtime/wt/general-3`，复用总控已完成的 Acquire → ReuseOnly → mini Bootstrap。
- Lease：`6bceb673-794e-4be5-a1c7-8b98b52b8126`；session `mini-ui-20260905001548`，未额外 Acquire。
- `DEPENDENCY_MODE=REUSE_ONLY`，`DEPENDENCIES_REUSED=true`，`INSTALL_INVOKED=false`。
- Inspector 前后均 `RESULT=PASS`；Skill hash `fb8f38d1a8f4237155b850e2d49e3ed264a99dff3b1394aae7152095a1cf0ea8`。
- Checkpoint message：`fix(miniprogram): align event sheet colors and today markers`。

## 实现与行为变化

| 需求 | 结果 | 主要实现 |
| --- | --- | --- |
| 4 | 调用侧及内容滚动 PASS；依赖 A 内核集成 | workbench 唯一事件 UiSheet 使用 `size="half"`，保留标题、完成、遮罩及 retry/close 调用。事件组件 virtualHost 去掉中间宿主高度边界；元信息、80 条时间线和最后人员链均在唯一 `scroll-view scroll-y type="list"` 内。 |
| 7 | PASS | 新增和编辑使用同一 `shift-color-picker`，`value/disabled`、可选 `largeText`，`change.detail.value` 为大写六位 HEX。预设点击和 HSV 拖动更新当前草稿预览；HEX 在应用/键盘完成时校验；仅展开/收起不会触发保存。最终新增、保存及失败保留草稿继续使用原控制器写入流程。 |
| 8 | PASS | `calendar-date-marker.wxss` 提供固定 18×18 view 与 text 居中契约，月/周均复用现有 `--ui-color-today-marker`（`#F5C518`）和 near-black（`#16202A`）；保留独立蓝色选中框。未改日期、时区、节假日、周末、跨月和班次逻辑。 |

颜色基准完整读取 `apps/web/src/features/scheduling-config/ShiftColorPicker.vue`，Web 未修改。
预设顺序为 `#0A66D5`、`#287D70`、`#4C5BD4`、`#9A6A13`、`#C33D56`；自定义默认 `#7A4FD6`。
三位 HEX 与当前 Web 一样拒绝；trim、可省略 #、大小写统一；非法输入不会污染有效草稿或指示器。
纯 HSV/RGB/HEX 与颜色预览对比计算只存在于 `shift-color-picker/color.ts`。
预览只提交目标 `shiftDrafts[index]` 的颜色、文字色和对比状态，不重建其他班种。

移动端按 Web max-width 640px 规则：HEX 占左半，应用下一行满宽且至少 44px；legend 10px、调色标签 9px、HEX/应用 12px。
色板 92px，色相可见轨道 18px、指示圆 20px，透明绝对定位 hitbox 44px，不扩大可见布局。
正常字号轨道到 HEX 标签间距实测 9px；大字另测 16px 输入，不把大字 fixture 当普通黄金截图。

## 引入点与根因

通过 `git log -S` 和相应 `git blame` 逐调用点核对：

- `0792ed01`：事件记录接入；meta/chain 位于独立滚动区外，事件 Sheet 未区分 half。
- `38233039`：排班配置迁移；仅有 56px 宽的 HEX 输入，已有颜色 handler 未连接可复用调色器，原输入可直接写入非法草稿。
- `1f715c96`：月历 PoC；today 背景为 primary，flex 与 padding 直接放在 text。
- `ad4cfb2c`：周历最初使用蓝色和 text flex；`50c6d1ed` 后续覆盖为黄色，未修复外部正方形容器。

本轮为明确行为修复，不声称语义等价重构。控制器 `this` 接收者、异步保存/catch、版本和 operationId 路径未变。
移除的是本任务引入的 workbench 全文件格式化噪声；该文件最终仅 4 行新增/2 行删除，`workbench/index.ts` 与 Base 完全一致。

## 验证

证据均在 ignored canonical `runtime/codex/mini-ui-20260905001548-c/`；不提交截图、日志、dist。
实际运行命令的 shell cwd 均为 `runtime/wt/general-3`，Vitest 通过 `pnpm --filter @schedule/miniprogram exec` 使用正确包 cwd。

- 基线：calendar-poc、calendar-simulate、exp-feat-002-event-records、p8-organization-c2 与其 controller：5 文件/24 项 PASS，48.66s wall time。
- Red：新增组件/布局需求 38 项失败；真实 controller 2 项失败/2 项通过；旧实现接受 ` ffffff `、`0f766e` 等未规范输入。后续移动布局回归再次先失败（实际 13px 与 Web 10/9/12px 不符）。
- 定向最终组：上述原有文件，加 3 个 C 回归文件及 thin-page-boundary、p8-component-registration、organization-direct-pages：11 文件/88 项 PASS。
- 最后移动样式修正后，仅重跑 `mini-ui-c-layout.test.mjs`、`mini-ui-c-color-picker.test.mjs`：2 文件/34 项 PASS，8.15s。没有再跑全量。
- TypeScript：`pnpm --filter @schedule/miniprogram typecheck` PASS。
- ESLint：4 个改动 TS 与 4 个 C 测试精确文件，`--max-warnings=0` PASS。
- `pnpm smoke:check-core` PASS：没有触及 Web 核心链路；没有调用 `pnpm smoke:browser`。
- 静态构建：`pnpm --filter @schedule/miniprogram build` PASS，288 文件；package audit PASS。最终 build/package 2.64s。
- 包体基线：main 1,715,719 / organization 1,053,334 / total 5,151,893 bytes。
- 最终开发树构建：main 1,717,383 / organization 1,069,434 / total 5,169,657 bytes。main 超过内部 1.5M warning 已在基线存在，硬门禁通过。
- 最终最大文件：workbench/index.js 219,782；manual/index.js 187,720；group-settings/index.js 187,176；directory-panel-controller.js 186,192；swap/index.js 183,603 bytes。workbench TS 未改。

颜色实际 Node probe 与测试均证明以下颜色经 HEX → RGB → HEX、HEX → HSV → HEX 完全相等：
`#000000`、`#FFFFFF`、`#FF0000`、`#00FF00`、`#0000FF`、`#808080`、`#F0F1F0`、`#0F766E`。
`#0F766E` 实测 RGB `(15,118,110)`，HSV `(175.3398058252427,0.8728813559322034,0.4627450980392157)`，两条路径均回到 `#0F766E`。
另外覆盖 hue 0/360/-120、零饱和度、明度 0/1、RGB 越界 clamp 和非法 HEX。

触摸测试覆盖：真实组件 SelectorQuery 入口；非零 rect 偏移/尺寸；client 坐标及 page 坐标减 viewport scrollOffset；SV 四边 clamp；start/move/end；结束先于异步测量返回时应用最后坐标；第二手指被忽略；cancel、resize、detach、disabled 使未返回测量失效；下一手势重新测量；父级颜色回传不重置黑/白或红色端点 HSV。
新旧班种保存测试验证只在原保存动作调用写入 client，带原 rules/version/operationId；保存失败保留颜色；展开/收起不写接口或 storage。

布局使用现有 miniprogram-simulate + playwright-core + 已安装 Edge，仅一浏览器，按需开关 `SCHEDULE_MINI_UI_C_LAYOUT=1`；`SCHEDULE_MINI_UI_C_EVIDENCE` 指定上述 ignored 目录。
320×844、390×844（既有黄金视口）、414×896、844×390，DPR 3、普通/大字、resize、reduced-motion 均通过几何断言。
一位/两位日期标记 18×18，dx/dy 均 0；黄色/深色与选中框并存；色板指示器在界内；HEX/应用不相交；移动应用宽度差 0；HEX 半宽差 0；hitbox 不遮挡 SV 或 HEX 输入；80 条事件滚动末尾和人员链可见，头部坐标不变。
原生 input 在 simulate 中不会绘制文字，因此截图用其真实组件绑定值创建浏览器 input 镜像，并断言 `#0F766E`；它不是微信原生输入框截图。

## 集成交接与限制

- 先集成 A 的 UiSheet size/default/header 手势和窗口高度逻辑，再集成 C。C 的 half 高度是调用契约；此槽未修改、未验证 A 的内核。fixture 对内容滚动使用确定高度边界，不冒充原生 50% 高度验收。
- C 仅改 workbench WXML/WXSS；总控保留最新图标迁移与 D 的公共通知接入，语义合并相邻 WXML 和 scheduling-config controller/JSON，不用 ours/theirs。
- 没有新增任何 `web-*.svg` 引用；此旧 Base 的既有引用由总控图标分支保留迁移。本任务不自行迁移图标、不修改 lineage policy、token 生成文件或 Web。
- 提交文件：workbench `index.wxml/index.wxss`；calendar-cell `index.wxml/index.wxss`；shift-event-records `index.ts/index.wxml/index.wxss`；`styles/calendar-date-marker.wxss`；scheduling-config-panel `controller.ts/index.wxml/index.wxss/index.json`；direct page `scheduling-config/index.json`；shift-color-picker `color.ts/index.ts/index.wxml/index.wxss/index.json`；4 个 `mini-ui-c-*.test.mjs`；本报告。
- 未测真实 Console/Network、冷启动、微信原生滚动和触控热区；未取得独立 Xiaomi 14 当前构建逻辑视口。没有新的确定性原生截图比较基线，不声明像素相似度百分比。
- 未宣称 Xiaomi 14 体验版验收通过。没有 push、版本分配、上传、release、生产连接/备份/部署或数据库操作；租约保留给总控收口。
- 唯一下一步：总控集成 A/C/图标及 D 的公共文件，统一运行正式全量门禁和已授权体验版流程。本 C 分支在本地独立提交后停止。
