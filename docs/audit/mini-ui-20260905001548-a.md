# mini-ui-20260905001548 — 任务 A

- 范围：通讯录需求 1–3、UiSheet 公共内核、通讯录筛选 three-quarter 接入。
- 分支：`codex/mini-ui-20260905001548-a`；干净起点：`32792fe47b1c3769bebe14a0632e3de9279e7b32`。
- 独占槽：`runtime/wt/general-1`，沿用总控 lease，不额外 Acquire、不安装、不 release。
- `L1` inspector PASS；Skill hash：`fb8f38d1a8f4237155b850e2d49e3ed264a99dff3b1394aae7152095a1cf0ea8`。
- 原 token 的 ReuseOnly 为 READY_REUSE；mini Bootstrap built=0/reused=3，INSTALL_INVOKED=false。
- 本轮按用户明确授权直接实现；全局状态、工作台、Web、依赖、图标 catalog、版本与上传由总控负责。
- 本地 checkpoint message：`fix(miniprogram): refine directory layout and shared sheet behavior`。

## 来源与引入点

已对修改调用点运行 `git log -S`、`git blame`，并读取当前实现：

- `2751f549` 将通讯录 placeholder 改为冗长搜索说明；本轮取 Web
  `apps/web/src/views/directory/InternalDirectoryView.vue` 的两条现行短文案。
- `1de042b5` 的卡片 `.entry-meta` 使用右侧 ellipsis，窄幅会丢掉路径末级。
- `6b5b30fb` 的卡片引用 `web-directory-phone.svg`。已只读核验图标迁移分支
  `codex/icon-parity-current-20260904`，实际 SHA 为 `297ad3d2f9cdd94507c1e7abd38302f27073a54e`：
  首页详情 WXML 与拨号动作均引用 `/assets/icons/ui-phone-success.svg`，该文件是既有生成资产。
- `304d742f` 建立 UiSheet 默认 78vh/max-height 660px 与共享 WXS 阈值。
- 通讯录私有筛选壳来自 `6b5b30fb`；`73811f1f` 的独立 controller 保留了私有窗口高度/resize 路径。
- 额外读取工作台现有 `hidden` 挂载条件，发现地址可能在隐藏 workspace 中首次测量；只在通讯录内部
  传递 `layoutActive`，激活 workspace 或切换科室/人员 Tab 后重测。

## 实现与行为变化清单

| 需求           | 实现                                                                                               | 自动验证结论                                                           |
| -------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 1 搜索文案     | 每个 pane 仅一份 `searchPlaceholder`，placeholder/aria 共用；16px，缩小内边距与间隙，窄幅 ellipsis | PASS：文案、事件绑定、无障碍契约及真实 CSS 几何                        |
| 2 电话同源     | 长号/短号共用循环引用 `ui-phone-success.svg`；拨号事件、号码、44px 点击区保持                      | PASS：调用侧；资产由总控集成 `297ad3d2`                                |
| 3 地址末级优先 | `directory-entry-card/tail-path` 单职责组件，以原生 SelectorQuery 测量候选宽度，选择最长可容纳后缀 | PASS：宽幅原样、窄幅后缀、LTR 括号/分隔符、原始值/aria、隐藏激活与清理 |
| 4 UiSheet/筛选 | `size=default\|half\|three-quarter`，`swipeArea=header\|handle`；通讯录 75%/handle-only            | PASS：尺寸、窗口监听、手势、固定头/重置、单一滚动区和恢复              |

公共默认仍为 `size="default"`、`swipeArea="header"`、`swipeDismiss=false`。half/three-quarter 按
`Math.round(wx.getWindowInfo().windowHeight * ratio)` 设置边框盒高度，覆盖 max-height；不重复扣除状态栏、
导航、胶囊或 TabBar。safe-bottom 只在内容 padding 内，default 的 78vh/660px 原样保留。

下滑起点只在 header/handle 的绑定区判定；内容区没有 WXS 手势绑定。沿用 8px 起动、1.2 轴比、96px
距离或 28px+0.65px/ms 快滑阈值及原回弹。重开会取消旧动画 generation，失去面板/卸载后不发迟到关闭。
窗口 on/off listener 成对注册清理；缺少 off API 时不注册。内容遮罩拦截触摸，通讯录底层 `scroll-y`
随 `filterSheetOpen` 关闭/恢复；筛选内只保留一个 scroll-y，原展开状态、滚动恢复和筛选数据结构保持。

删除已无调用的 `filter-sheet-drag.wxs`、私有壳 CSS、旧高度计算/resize 注册和专属 swipe close 包装；
均可从 Git 历史恢复。外置 Sheet 宿主继续继承通讯录 token 和大字号样式。

语义审查：搜索函数、防抖/回车、接口、权限、收藏、拨号、分页和状态恢复逻辑未改；关闭继续调用
`closeFilters`，事件 dataset 保留 directory kind。新布局回调保留接收者绑定，使用 revision 和 attached
校验丢弃旧测量；不使用 reverse、字符数裁剪、DOM/window API、额外持久化或新依赖。

## 实际验证

所有 shell 的初始 cwd 均为独占槽，Mini 测试在其 `apps/miniprogram` 下运行。日志/截图位于已用
`git check-ignore` 验证的 canonical `runtime/codex/mini-ui-20260905001548-a/`。

- 修改前：6 文件/73 测试通过；Mini TypeScript、领域 ESLint、production 静态构建和包审计通过。
- 首轮新回归：14 项中 11 项在旧源码失败、3 项通过（`red.log`）。隐藏挂载补充回归也先失败
  （`hidden-path-red.log`），随后转绿。
- 最终领域矩阵：10 文件/101 项全部取得通过证据。包括 UiSheet、通讯录 controller/query/native/card
  simulate/gesture/boundary、diagnostics correction、thin-page guard 及新增 17 项任务 A 测试。
- `targeted-final.log` 的单次集合为 100 pass/1 既有构建 fixture 超时（默认 5 秒）。该 fixture 单独默认
  运行曾 1.252 秒通过；最终单独命令 `node node_modules/vitest/vitest.mjs run
scripts/directory-diagnostics-correction.test.mjs --fileParallelism=false --testTimeout=30000` 为 6/6 通过，
  构建 fixture 745ms。只调整本次命令预算，没有改全局 timeout 或断言。
- 最终 TypeScript、领域 ESLint、Prettier、`git diff --check` 和 `pnpm smoke:check-core` 通过；未触及 Web
  核心链路。未运行全量聚合测试。
- 最终 `node scripts/build.mjs --profile=production` 成功：286 文件，1795ms；包审计通过。
- 包体：基线总包 5,151,893 bytes / 主包 1,715,719 / organization 1,053,334；本轮构建总包
  5,150,799 / 主包 1,718,800 / organization 1,049,159。其他分包无变化。主包超过内部 1.5M 的既有 warning
  保留，未到阻断阈值；最大文件分布另存 `baseline-largest-files.log`。这是工作树构建记录，不是最终上传包。
- simulate 不把 Mini ARIA 属性映射到 HTML DOM；实际组件组合验证完整 value/可见文本/收藏/拨号，ARIA
  由单独 WXML 契约验证。期间修正了测试文件 URL 和标签选择器的不适用用法。

## 浏览器样式几何证据

复用已有 `playwright-core` 与本机无头 Edge，仅一个实例，无新依赖。`geometry.mjs` 读取实际源 WXSS、
token 与路径纯函数，使用 HTML 节点代替原生 view/text/scroll-view；15 个场景通过，结果在
`geometry.json`，截图为 `layout-*.png` / `sheet-*.png`。非截图相似度、非微信原生运行时证据。

| 视口宽度 | 人员文案实测宽度 | 输入区宽度 | 结果                                             |
| -------- | ---------------- | ---------- | ------------------------------------------------ |
| 320      | 320px            | 260px      | 尾部省略，完整 aria 保留                         |
| 390      | 320px            | 322px      | 完整容纳                                         |
| 414      | 320px            | 346px      | 完整容纳                                         |
| 393      | 320px            | 325px      | 额外逻辑视口完整容纳，未冒称已核验的小米实测视口 |

包含 320 大字号地址、844×390 横向窗口、default/half/three-quarter、1100px 高窗中 three-quarter 超过
660px、头/重置滚动前后坐标不变、内部列表到底、页面不移动、无横向溢出。浏览器 safe-area env 为 0；
非零设备安全区仅由边框盒/padding 契约与窗口 fixture 覆盖。系统字体/真实安全区/原生触摸仍属未测层级。

## 总控集成处理

1. 先纳入既有图标分支再语义合入 A；保留迁移后的其他图标路径，并确保电话仍为 `ui-phone-success.svg`。
   A 的旧基线没有该 canonical 资产，本轮未复制 SVG、未恢复 web-phone、未改 catalog/generator。
2. A 先于 C；C 的事件记录可使用 `size="half" swipe-dismiss="{{true}}"`，默认 header 手势。
   仍使用受控 visible 与 `close` 事件（source=button/backdrop/swipe）。
3. 最终源码一致性、全量聚合、官方图标清理、版本分配/上传统一由总控处理；本任务仅本地 commit，lease 保留。
4. 未推送、未上传、未提审、未正式发布、未部署 ECS/数据库、未迁移数据库；未宣称小米 14 真机验收通过。
