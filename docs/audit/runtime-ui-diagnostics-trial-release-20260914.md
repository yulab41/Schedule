# 跨微信实例 UI 诊断增强体验版交付

## 不可变身份

- 体验版：`0.1.0-p10.20260914.133`
- Git：`2d7f685a599254daefa0a770b7e9a1bff248972b`
- 说明：`运行时UI诊断增强 2d7f685`
- profile：`production`，clean
- 上传 Manifest：`eb30227698406cf14dd08b62ba1c547322be1d668aa5f55f47f19a37616b90b0`
- 上传时间：`2026-09-14T10:46:08.054Z`

远端轻量 tag `miniprogram-trial/0.1.0-p10.20260914.133`、本地 allocation、Manifest 和脱敏 receipt 均绑定同一版本与 SHA。上传使用仓库 Node `miniprogram-ci`，未控制微信开发者工具。

## 本轮内容

- 测试工具页使用固定首屏和显式纵向 `scroll-view`，异常实例即使正文不能滚动也能看到并复制首屏结果。
- 首屏自动检测受控 Grid 双列、跨组件 CSS 变量和滚动 viewport/content；并列显示 CSS 加载圈与 SVG 图形。
- 报告升级为 v2，增加运行时兼容性摘要。业务页面、API、权限和数据未因诊断探针改变。
- 为满足累计体验版血缘，候选同时包含 `origin/main@4179f05a` 的导出选择变化后重置文件状态修复。

## 自动化证据与边界

- 合并后诊断/导出专项 69 项通过；Mini 完整 173 个文件、1187 项通过、16 项跳过。
- TypeScript、production 构建、包体、确定性和 `smoke:check-core` 通过；正式上传生成 363 个文件。
- 完整 Mini `verify` 唯一失败仍为本轮未修改的手动排班页面节点预算：实际 `1507`，既有 no-growth 上限 `1506`。本轮未修改手排页面或放宽预算。
- 上传成功不能代替微信原生或小米 14 验收；根因必须由正常、异常微信实例返回的同版本探针结果确认。

## 发布边界

- 已上传体验版；未提交审核、未正式发布。
- 未追加服务器版本 allowlist，未部署 API/Web，未连接或修改生产数据库。
- 旧体验版本及其远端 tag 均保留。

## 同版本真机取证

在正常和异常微信实例分别确认 `.133 / 2d7f685` 后进入“更多 → 测试工具”，截取首屏兼容性卡片，点击“复制首屏诊断”，再验证页面能否继续向下滚动。只有两份同版本结果返回后，才判断 Grid、CSS 变量、图形绘制或滚动容器中的具体差异。
