# Feedback15：日历与通知设置八项修复

## 基线与范围

- 用户批准八项计划，月历62px、授权单按钮两步；实施基线a8695f2a（应用6b8a9e9c），保留111交付记录。
- 独占general-4；Acquire/ReuseOnly/root Bootstrap/定向测试通过，无依赖安装。基线Mini verify通过，Worklet2/2，主包1705422/总包4536468字节；原有主包与手排节点提示保留。
- 首轮日历定向55通过；新增行为RED6失败/50通过后GREEN56通过。岗位路由/客户端RED3失败后GREEN6通过；弹窗RED1失败后GREEN4通过；两步授权RED1失败后联合33通过。

## 八项实现与引入点

1. 周历：e94a54ca引入三周最大估算/全局选择器；8f441d2d缓存仍包含三周，点选仍全量下发面板。改为当前周内容/群/账号/宽度缓存，渲染回调后测量当前七列，最高列底部24px；内容禁止flex收缩，序列及上下文校验拒绝旧结果。点选只下发选中格路径与详情。自动化覆盖邻周更高、迟到测量、重复点选、跨月周。
2. 未上班：e94a54ca仅rest/done折叠，本轮before也默认折叠；状态边界、手动覆盖和导航重置保留。
3. 岗位改名：新增版本化/幂等PUT，复用组织事务与权限；同ID改名并提升岗位/群规则版本。弹窗预填、错误重试保留输入及operationId，保留未保存成员勾选；失效旧日历缓存。日历已有实时岗位名称查询，不改历史排班记录。
4. 月历顺延：e94a54ca按上一轮要求只显示默认班种。本轮最新批准规则替代：优先默认，缺失则取筛选内按班种/槽位排序首项。徽标、姓名、人数及变更同源。
5. 月历高度：e94a54ca将外框改62但模板默认仍54；当前工作台显式传62，五行310/六行372，其他调用方行高不变。旧布局脚本硬写62未覆盖真实模板，本轮simulate使用真实WXML与控制器数据。
6. 补班：工作台模型原仅投影isOffDay，丢失isWorkday。贯通月/周/列表及访客模板；蓝底班徽标来自节假日数据，不硬编码日期。
7. 授权：22707cf6引入五类独立入口；本轮单按钮按3+2分步，部分拒绝继续、调用失败当前步重试，逐类结果、再次授权、上下文清理；原开关仍只控制接收偏好。保留底层单模板能力，不更换模板或发送真实通知。
8. 今日圆圈：121714b1引入18px view容器；文字仅设line-height，未约束自身布局高度。本轮显式18px文字盒/不收缩，与圆形容器同中心；浏览器检查1/12，Skyline字形视觉仍待真机。

## 验证记录

- 真实MySQL：calendar37通过（含改名后历史/访客同步、排班表完全不变）；scheduling-config9通过（权限、跨群、删除、空白/超长、幂等重放、内容冲突及并发单赢家）。首轮测试夹具操作ID头/正文不同导致400，修正夹具后复测9通过。
- 周历运行时32通过；新增实测序列验证渲染前不测量、只选择当前周、邻周更高不撑高、迟到结果不覆盖，选中态路径回归通过。
- 390/320真实WXML、真实ui-button及生产CSS浏览器几何通过：月历额外留白0、行高62，周历最长列底边距24，今日文字盒居中。布局检查发现旧role-heading子view列布局覆盖新按钮容器，增加精确行布局规则并用左右顺序断言验证。
- 证据与截图仅存ignored runtime/audit/feedback15，全部为合成数据；simulate/浏览器不等于小米14原生验收。
- pnpm verify完整链路通过：format/lint/build/typecheck/icon parity、Mini1184通过/16跳过、根1252通过/439跳过、依赖保护81通过。跳过项不算通过；本轮显式浏览器几何另行执行通过。
- 运行/浏览器验证：pnpm smoke:browser（同仓库node入口，本地API3105/Web4175）已通过完整流程；初次preview缺开发登录按钮，按既有流程启用本地dev后通过。不会将此作为原生证据。
- 通知账号切换补充RED1→GREEN34联合通过：返回时重读当前账号配置，拒绝旧授权结果并解除旧busy。全量首轮仅旧单模板源码断言失败，现按批准3+2行为更新，未移除上限或鉴权验证。
- AST对照旧blob51704f3e确认13个生命周期/读取/视图/点选函数变化，新增setCalendarData；导航日期、队列、this绑定和回调次数不变，只有批准的测量时机变化。更新精确blob证明，保留所有required checkpoints。
- 收口补充：微信授权成功但偏好保存失败不得提前推进步骤，RED1后最终通知/边界35通过；该Mini局部改动重新通过格式、ESLint、类型和Mini verify。最终Worklet2/2、主包1708859/总包4550421字节，保留既有内部预警。
- 本地smoke临时服务停止，synthetic local-admin原标记独立回读恢复；无生产连接、真实通知或上传。最终smoke:check-core通过。代码/自动化已完成，原生待用户复核。

## 交付边界

- 八项实施合并为检查点fix(calendar): resolve feedback15 calendar and notification issues；验证全部通过后提交推送。
- 本轮未授权生产部署、备份、体验上传或版本放行。完成代码交付后报告精确SHA再取得对应授权，不分配下一体验版号。
- 小米14待同版本复核周历点击/翻周、月历高度/顺延/圆圈、未上班折叠、岗位改名同步、补班和两步授权；原导出/二维码问题不在本批扩展。
