# MINI-UI-ALIGN-009 独立只读复核

复核目标为图标前置树 `297ad3d2` 到集成 `454ad56e` 及总控电话清单增量。独立代理对用户25项清单
逐项核读实际源码；24项未发现高置信新增问题，颜色连续触摸发现一处P2，修复后独立复现确认关闭。
没有将前轮已完成 Web/lockfile 差异误记成本轮新增，也没有以缺少真机证据阻断自动交付。

## P2 / REVIEW-COLOR-001：连续点击等待测量时丢失最新选择

- 引入点：`121714b1`，经 `git log -S`/blame确认；文件
  `apps/miniprogram/src/subpackages/organization/components/shift-color-picker/index.ts` 的 startGesture。
- 原因：第一次touchend后异步SelectorQuery尚未返回，旧gesture仍存在；旧guard把第二次完整点击
  当作额外手指拒绝。独立内存复现中，两次点击白→黑只有1次查询，最后却为白色。
- 修复：只阻止尚未ended的活动手势；新完整手势替换已结束的待测量对象，继续复用回调对象身份校验
  丢弃旧响应。活动手势、多指、this接收者、API调用、保存/取消和颜色转换没有改变。
- 新增5项回归：SV/hue分别测正序/倒序响应共4项，旧实现4红；活动第一手势忽略第二手指1绿。
  修复后38项picker+6项controller共44通过。未改变超时阈值或削弱原断言。
- 独立复核复跑自己的复现：两种回调次序都是2次查询，只输出最新黑色一次；旧白响应被忽略，
  手势状态正确释放，多指仍忽略。结论：P2关闭，无新增问题。
- 该行为修复单独checkpoint：`fix(miniprogram): retain latest rapid color gesture`。

## 运行与影响面复测

- 修改前的正式根 `pnpm verify` 一次通过：Mini 758 passed/11默认布局skip，Node22/22，root1173 passed/364既有集成skip。
- 修复后仅重跑受影响的Mini格式/lint/TypeScript、Mini全量：131文件通过+1默认布局文件skip，
  763 tests通过/11默认布局skip，97.18s；trial-lineage 15/15，原默认阈值下竞争测试4.489s。
- Mini production verifier重跑通过：source/built worklets2/2，total5,208,175、main1,757,754 bytes，
  确定性manifest `16c7fe1a51a217833b360ed7c714d1425747c57e89f16e5bc07d74c08cc0a76a`。
- CI dry-run通过：`94d48499b9609ff4f9f85b9afeadcb0bc48757d9175894ca1a3839f35bd8847d`，无外部写入。
- C浏览器布局在修复后单独启用重跑1项通过；此前集成树B/C共13项实际执行通过，补全普通suite默认skip的11项。
  A15场景、B110状态、D7场景源码未被该修复改动，证据继续有效。
- 不重复不受影响的Web/API/root测试；最终上传仍从最终clean main SHA重新冻结构建/manifest。
- 证据在 ignored `runtime/codex/logs/mini-ui-20260905001548/review-*` 与 `final-layout/c-reviewed/`。
  全部为Node或浏览器代理证据；未宣称微信原生或Xiaomi14验收通过。
