# 五类微信通知模板接入

用户已提供并确认四个实际业务模板，保留原值班提醒，共五类；2026-09-12微信只读模板列表核对成功。当前模板不再共用单一业务映射。

## 用户确认的内容与授权入口

- 排班更新：date2为完整排班开始~结束年月日，thing7发布状态“新增”，thing4为月日范围排班已更新摘要，name3为本次操作人，不能用接收人姓名替代。
- 换班：short_thing1为操作者，short_thing4固定“见换班岗位”；thing3为完整姓名+MMDD+班种缩写→完整姓名+MMDD+班种缩写。用户确认超20字符时显示“人员姓名过长，点击查看换班详情”，不截断姓名；time2保留完整年月日范围，thing5为处理状态前缀+原因摘要。
- 加扣班：thing3班次类型、time4替班日期、thing5状态+备注、thing2本次操作人；沿用用户指定“替班通知”模板。
- 请假：name1请假人、thing2请假类型、thing3请假事由、time9原申请北京时间、thing16处理状态及查看提示。
- 当前换班业务不收集普通申请原因时显示“未填写原因”，不编造原因；原有事件/撤销原因存在时按事件取值。
- 通知设置页五个独立授权按钮，每次只请求选中模板；无自动连弹。拒绝单项不覆盖其他原生授权或接收偏好。原值班诊断仍只订阅值班模板；其他UI/图标保持。

## 数据与兼容

- 五个族的现有业务事务显式传入authorization.user.id。启用对应模板时，在通知创建事务内记录操作人、对象、日期/班种、申请时间和处理状态快照；重试不随后续改名、对象修改漂移。
- 按notificationType、objectType和可信payload来源路由发布、审批、换班、加扣班、请假。一次发布每个实际接收人一条；原有接收人/权限、草稿与静默不发、去重和重试规则不变。
- 配置缺失/格式不匹配/重复模板关闭对应通道，不阻断业务事务；发送前继续校验群组/成员/接收偏好。旧泛用业务模板配置兼容；不扫描、补发旧通知。
- 新增三组ID/FIELDS环境变量并透传API/定时任务；原BUSINESS为排班更新，DUTY_REMINDER保留。配置接口增加可选三个模板ID，旧客户端解析仍可工作；新授权按钮需要新体验版。
- 无数据库迁移、依赖新增或真实发送测试。实际模板ID及群组ID仅进入受控服务器配置。

## 验证与交付

- 引入点git log -S / blame：7842c4da原业务映射只有通用字段，尚未支持用户此次实际字段/分模板需求。
- UI RED 2失败/25通过→GREEN59；换班最终紧凑格式RED1失败/3通过→GREEN4。API/Mini类型及限定lint通过。
- 隔离MySQL六文件132通过，包括四模板快照/改名不漂移/事务回滚/重试去重与发布、换班、加扣班、请假原业务回归；网关完全模拟，非真实收信证据。
- 五按钮生产CSS浏览器夹具在390×844/320×844、已配置/未配置四状态通过：按钮高46px、标签完整、无横向溢出，最后按钮可随页面纵向滚动访问；非WXML/Skyline或小米14验收。证据general-5/runtime/audit/template-subscription-ui-layout。
- 运行/浏览器验证：pnpm smoke:browser（等价node --env-file与本地env内存适配，API3105/Web4175）通过完整登录/管理员/成员/访客/访问记录；合成local-admin原标记已回读恢复，自建服务已停止。
- 全量检查结果由收口状态补充。提交标识feat(notifications): separate five subscription templates；随后按已授权服务器部署/配置，体验版上传需具体候选授权。不得用模拟网关通过声称真实自动收信通过。

全量pnpm verify已完整通过：format/lint/build/typecheck/icon parity、Mini1164通过/15跳过、根1251通过/434跳过、依赖保护81通过；434包括未配置本地隔离数据库时跳过的用例，相关132已另行真实MySQL验证。原生和实际微信发送仍未验证。

## 服务器已部署并启用五类模板

- 代码22707cf61e8c0247755eb17a85d9c0dfbe37e0fb已提交推送并完整部署API/Web，schema57，无新增迁移。部署前即时live为d1d7a7a3；新配置仅原子修改四类业务模板的8个ID/FIELDS键，其余配置及root:root/0600保持。
- 备份a62363df-2e39-4bf6-95c3-8a6f057f8889，54表、105975620字节，SHA256 80b8da25c0d8f25eb61978631067b57b741d970ebe7f99c4b213dd237bd84346与服务器实际加密文件一致。未上传本地数据库、会话或凭据。
- 实际运行API确认5个唯一模板，business/swap/dutyAdjustment/leave各自配置有效；通过现有WeChat网关只读逐项核对全部实际模板字段，合成数据构造通过，realSends=0。换班沿用用户原ID，通过已确认的thing3紧凑摘要和short_thing4“见换班岗位”解决，不修改微信平台模板。
- 完整生产verifier及allowlist verify通过；公网109/108=200，未知426。最近自然cron两轮：值班created0/duplicate11/skipped0、retry attempted0/sent0/failed0/skipped0；未补发历史通知，未人为制造业务事件或发测试消息。
- 新界面五按钮尚未上传。已对22707cf6应用候选弹窗申请新体验版上传及追加放行，当前未收到该项回复，未分配版本或预约tag；现有109仅原订阅入口。实际自动收信和原生五按钮仍待后续授权/验收。
- 文档收口标识docs(ops): record five-template activation；若按项目规则同步文档release，仅通过可信hash-identical复用，应用内容仍22707cf6，无重复应用重建/迁移。运行证据位于本槽ignored runtime/audit/feedback12-five-release。
