# Skyline 3.17.2：月历/日期分页改原生滚动（体验版 155）

日期：2026-09-17　作者：Codex　授权：用户当次报障 + 既有「可自行提交、放行、上传」授权

## 用户报告（均只在 3.17.2；3.17.3 正常）

1. 请假页年月日选择器：定位按钮落下的是**临近月**而不是当月，且**没有跳转动画**；要求是"跨多个月也只做一个月的跳转动画"。
2. 请假页选择器左右切换仍会**反跳**。
3. 首页月历定位按钮同样问题（临近月 + 无动画）。
4. 首页月历左右切换**无动画**、内容**不跟手**（手势滑动正常）。

同时用户确认：3.17.2 的滚轮"已经和 3.17.3 基本一样"，并且低速时末尾更顺滑——**这一条是优点，本轮不动**。

## 根因

两处的程序化切页都是"3 槽环形 `swiper` + 改 `current`"。3.17.2 无法动画化程序化跳转（并会反向重播圆形滑动），所以此前一轮把它设成 `duration: 0` 并手动补一次结算。后果正是本轮四条：**没有动画**（1、3、4）；而手动结算依赖"当前槽 == 目标槽"，一旦错过就丢结算 → 环形状态与画面错位 → **落到临近月 / 反跳**（1、2、3）。

## 实现（3.17.3 分支逐字未改）

按当轮确立的原则（最小改动优先；当低版本无法在原实现上适配时，为它设计匹配的实现而不是硬打补丁），把 **3.17.2 的分页换成原生横向 `scroll-view`** —— 与滚轮同一条已真机验证过的通道：

- 环形三面板、面板标记、插值/高度逻辑全部复用；面板体抽成 WXML `template`，swiper 分支与 scroll-view 分支**共用同一份面板标记**。
- `scroll-into-view` + `scroll-with-animation` 让平台自己动画**一个面板**；`bindscroll` 提供度量并驱动结算，手势滑动与程序化切换走**同一套结算代码**（所以跟手、反跳都消失）。
- 共享助手（度量合并、最近槽、面板 id、结算延时）放在 `calendar-period-pager.ts`，两个组件共用，不存在两套实现。
- 定位今天：先"无感归位"（同一月份内容，视觉无变化），再**单面板动画**滑到当月——跨多少个月都只滑一格。
- 3.17.3 仍走原 `swiper` 分支（`wx:else`），`swiperDuration`/`swiperCurrent` 行为与上一版相同。

## 验证（3.17.2 模拟器，逐帧取样）

| 场景 | 证据 |
| --- | --- |
| 请假页：打开 | `compat=true`、`target=date-pane-1`、环形 `[0:2026-11,1:2026-12,2:2027-1]` |
| 请假页：下一月 | 250ms 时 `left=8391.9`（动画中）→ 结算 `left=9072=2×4536`、草稿 2026-12 → **2027-01** |
| 请假页：定位今天（2027-01 → 2026-09，跨 4 月） | 250ms 时 `left=488.8`（动画中）→ 结算 `left=0`、草稿 **2026-09-17**（当月当日），**只滑一个面板** |
| 首页月历：连按 3 次下一月 | 2026-09 → **2026-10 → 2026-11 → 2026-12**，每次正好一个月，度量分别为 8728/4364、0/4285、4206/4206（均等于整数个面板） |
| 首页月历：定位今天（2026-12 → 2026-09） | 250ms 时 `left=623.7`（动画中）→ 结算 `left=0`、月份 **2026-09**，视口高度 310 不变 |

门禁：typecheck、Mini 全套 **1221 项通过 / 16 跳过**、package 通过（总 **4643899B**，较上一版 +11KB）、determinism `6ea2af8f…`、format、lint、smoke:check-core 全通过。

## 交付与放行

- 上传：`0.1.0-p10.20260917.155`（说明「Skyline 3.17.2 native scroll pager a402d01」，Manifest `683b76f3…9b63`），production/clean；上传前后检查 `RESULT=PASS`。
- 放行：可信 `ensure` 追加 `.155`（保留 `.154`）并通过健康与策略验证；`schedule-client-version-allowlist verify` 与 `ecs-verify.sh` 通过；公网 `.155=200`、`.154=200`、动态未知 `=426`。
- 未部署应用制品、未备份或迁移数据库、未提审、未正式发布。

## 唯一下一任务

小米 14 打开 `.155` 复核：请假年月日选择器的定位与左右切换（单月动画、不反跳、落到当月）、首页月历的左右切换与定位（有动画、跟手、落到当月），并确认 3.17.3 实例无变化。

## 追加：`.155` 的单元格宽度回归与 `.156` 修复

用户回传：月历页面单元格宽度严重错误，月历无法正常显示（可在开发者工具目视复现）。

- **现象**：月历每行只显示一列（日期是 31/7/14/21/28 = 每周一），单元格宽度等于整卡宽度；请假页日期网格同样。
- **根因**：在原生横向滚动容器里，3.17.2 无法为"环轨道 / 面板"解析百分比宽度链，面板宽度退化为不确定值，于是 `.month-grid { width:100% }` 与 `.calendar-cell-slot { width:14.285714% }` 都落到 `auto`，单元格按内容撑满整行。
- **排查证据**：先试 `flex: 0 0 100%` 无效；`outline` 在 Skyline 不渲染（描边全无）；`background` 上色也未见——说明该路径下 WXSS 变更不可靠/百分比无法解析，于是改为**测量后内联 px**。
- **修复**：新增共享助手 `measureCalendarPeriodPaneWidth`（`calendar-period-pager.ts`），在两个组件的兼容分支里各自量一次容器宽度并以 `style="{{paneStyle}}"/"{{datePaneStyle}}"` 内联到面板；CSS 只保留 `display:flex` + `flex:none`。宽度是内联值，不依赖百分比链，也不依赖 WXSS 是否刷新。
- **验证（3.17.2 模拟器目视）**：`pages/calendar-poc` 月历恢复 7 列（1–30 + 国庆灰格、周六日红色）；手动排班页"开始日期"选择器日期网格恢复 7 列、31 号高亮正确。
- **门禁**：typecheck、Mini **1222 项通过 / 16 跳过**、package 总 **4644990B**、determinism `64d70ef5…`、format、lint、smoke:check-core 全通过。
- **交付**：`.156`（说明「Skyline 3.17.2 pager pane width f7155b4」，Manifest `6cc5901b…7a60`）由合并了当前 `origin/main`（仅含一个文档策略提交，未触及小程序代码）的候选 `f7155b4b` 上传，前后检查 `RESULT=PASS`；可信 ensure 追加 `.156` 保留旧版；`ecs-verify.sh` `[verify] complete`；公网 `.156=200`、`.155=200`、未知 `=426`。
- **注意**：`.155` 含本回归，请以 `.156` 为准（`.155` 仍在白名单中，但已被 `.156` 取代）。

## 追加：`.156` 的手势/方向问题与 `.157` 修复

用户回传：宽度已正常，但**手势左右滑动不能按月切换**，而是滑过 3 个月份的内容；定位按钮正常；左右按钮能跳月，但**动画方向相反**（3.17.3 正常）。

- **根因**：环形槽位会轮转，而原生滚动是按**物理位置**走的。`getAdjacentCalendarPeriodSlot` 会环绕（active=2 时"下一月"落到槽 0），于是"下一月"可能出现在左边 → 动画反向；同时一次手势可以拖过 3 个面板 → 看到 3 个月份内容。
- **修复**：兼容分支改为**物理顺序固定**——三块面板按 `relative` 固定为 `前 | 当前 | 后`（新增 `compatPanes`/`syncCompatPanes`、`syncCompatDatePanes`），滚动目标按 **月份方向** 取 `pane-0`（上）/`pane-2`（下）而不是槽位；每次结算后把内容轮转到中间并**无感归位**（`scroll-into-view` 到 `pane-1`、关闭动画、同步重置滚动度量）。于是：一次手势最多跨一个面板（= 一个月），方向永远与月份一致，环形槽位继续由宿主维护、互不干扰。
- **验证（3.17.2 模拟器，逐项取样）**：
  - 首页月历：`compatPanes.relative=[-1,0,1]`；按钮 Next → **2026-10**、Prev → **2026-09**，每次结算后 `pagerTarget=month-pane-1`、度量回中；模拟手势（滚到 pane-2 后抬手）→ **只前进 1 个月**（2026-09→2026-10）。
  - 请假选择器：`compatPanes.relative=[-1,0,1]`；Next → **2027-01**、Prev → **2026-12**，手势 → **只前进 1 个月**；每次归位中间、度量同步；日期网格仍为 7 列（截图确认）。
- **门禁**：typecheck、Mini **1222 项通过 / 16 跳过**、package 总 **4646206B**、determinism `1f933a47…`、format、lint、smoke:check-core 全通过。
- **交付**：`.157`（说明「Skyline 3.17.2 pager pane order 12d1a00」，Manifest `536497b3…ed2a`）上传，前后检查 `RESULT=PASS`；可信 ensure 追加 `.157` 保留旧版；`ecs-verify.sh` `[verify] complete`；公网 `.157=200`、`.156=200`、未知 `=426`。**请以 `.157` 复核。**

## 追加：`.157` 的闪烁/卡顿/快速手势阻塞与 `.158` 修复

用户回传：滑动卡顿；松手后**先闪一下滑动前的月份**再出现新月；高度自适应**要等闪烁结束才开始调**；快速连滑**被内容加载卡住**，必须等加载完才能再滑。

- **闪烁根因**：归位跳动原先写在结算里，会在**宿主持有的新面板渲染之前**先把滚动跳回中间面板——那一帧里中间面板还是旧月份，于是先闪旧月。改为**只在面板同步时归位**（`syncCompatPanes` / `syncCompatDatePanes` 在同一次 `setData` 里下发新面板 + 归位目标），两者同一渲染帧、不再出现旧帧；只有在"无步可提交 / 上一步仍在加载"时才单独归位（此时面板本来就该停留在当前月）。
- **卡顿根因**：拖拽过程中每个 `bindscroll` 都可能因槽位变化写 `viewportHeight`，等于逐帧 `setData`。改为**拖拽期间只记录状态，高度只在结算时设置一次**。
- **快速手势被阻塞**：上一步的月份数据还在加载时 `shiftPending` 会拒绝新的一步。现在把这次手势的**方向入队**（复用环形分页既有的有界队列），数据到达后自动接着走，并把滚动先归位，避免停在侧面板上。
- **验证（3.17.2 模拟器，连续取样）**：
  - 首页月历连续两次右滑再左滑：**2026-09 → 2026-10 → 2026-11 → 2026-10**，每次 `target=month-pane-1`、`compatPanes.relative=[-1,0,1]`。
  - 请假选择器连续两次右滑再左滑：**2026-12 → 2027-01 → 2027-02 → 2027-01**，随后按钮再前进 → **2027-02**，每次归位中间、面板顺序不变。
- **门禁**：typecheck、Mini **1222 项通过 / 16 跳过**、package 总 **4646321B**、determinism `2df84730…`、format、lint、smoke:check-core 全通过。
- **交付**：`.158`（说明「Skyline 3.17.2 pager no stale frame 8351312」，Manifest `63414cb6…4cc0`）上传，前后检查 `RESULT=PASS`；可信 ensure 追加 `.158` 保留旧版；`ecs-verify.sh` `[verify] complete`；公网 `.158=200`、`.157=200`、未知 `=426`。**请以 `.158` 复核。**
