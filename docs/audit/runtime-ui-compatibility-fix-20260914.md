# 小程序 Skyline 3.17.2 UI 兼容修复

## 结论

异常已定位为微信单实例使用的基础库/Skyline CSS 运行时差异，不是项目组件库版本漂移。关键对照均来自同一体验版 `0.1.0-p10.20260914.133@2d7f685`：

| 项目 | 异常实例 | 正常实例 |
| --- | --- | --- |
| 基础库 | 3.17.2 | 3.17.3 |
| Grid 双列 | 退化，顶部差 8px | 同行，顶部差 0px |
| CSS 缺口圆 | 截图为尖角 | 截图为圆弧 |
| SVG 完整圆 | 正常 | 正常 |
| CSS 变量 / 显式滚动 | 正常 / 正常 | 正常 / 正常 |

因此采用精确 3.17.2 兼容分支，不改变 3.17.3 及以上实例的原视觉代码路径。设计记录见 `docs/superpowers/specs/2026-09-14-miniprogram-skyline-3172-ui-compatibility-design.md`。

## 引入点

- 工作台 Grid/电话双列由 `d9296df0 fix(miniprogram): match p4 selected duty details` 引入。
- 基础 CSS 加载圈由 `24bc2c4b feat(miniprogram): add native foundation controls` 引入。
- 工作流 CSS 加载圈由 `bc32a4f1 fix(miniprogram): align p7 physical feedback` 引入。
- 这些提交在当时的运行时上是有效实现；本轮修复的是基础库 3.17.2 的渲染兼容边界，不回退原功能。

## 实现与行为变化

- 新增无依赖的运行时判定：仅 `SDKVersion === '3.17.2'` 返回 true；版本缺失、API 不可用或读取失败均保留正常路径。
- 仅在 3.17.2 下，将工作台/访客详情、通讯录卡片、平台账号、排班配置及统计面板的生产 Grid 容器切换为局部 Flex 布局。
- 仅在 3.17.2 下，将共享 `ui-loading`、工作台/访客加载态和请假/换班/加扣班工作流加载圈切换为本地 SVG；尺寸、颜色及动画节奏与原路径对齐。
- 诊断页和手势探针的 Grid 不替换，以免兼容代码掩盖诊断结果。
- 没有修改登录、成员/访客数据、日历计算、电话动作、工作流控制器语义、接口、权限、缓存或生产配置。

## 正常实例零视觉改动边界

- 3.17.3 的判定测试明确返回 false。
- 原 Grid 声明、原 CSS 圆环节点和原样式均保留；只有 3.17.2 才附加兼容 class 或进入 SVG `wx:else`。
- 因而源码层面 3.17.3 仍执行原布局与原加载圈。是否达到像素级不变仍需新体验版的小米 14 原生截图确认，不能用 Node 或构建结果替代。

## 验证

- RED：新增兼容回归初次运行 3 项失败；补齐主体后，工作流加载圈扩展用例再有 1 项失败，证明测试覆盖到缺失分支。
- GREEN：兼容回归 6/6；工作流宿主/直达页/生命周期/共享展示/提示 65/65；相关联合 46/46。
- Mini 全量：174 文件、1193 项通过；2 文件、16 项跳过。
- TypeScript、production build（366 文件）、包体审计和确定性检查通过；Manifest `55d0e5f3…911884`。
- 包体：主包 1,732,843 B，总包 4,600,497 B。相对体验版 133 构建记录的总包 4,591,628 B 增加 8,869 B（约 8.7 KiB、0.19%），没有新增依赖。
- `pnpm format:check`、`pnpm lint`、`pnpm smoke:check-core`通过。
- 完整 Mini verify 仍只被本轮未修改的手动排班节点基线阻断：1507 > 1506；独立 typecheck、build、package 和 determinism 已通过，本轮不放宽门禁。

## 验证边界与下一步

以上是静态、Node 和生产构建证据，不是微信原生验收。本轮未调用微信开发者工具、未上传体验版、未操作 allowlist、未部署生产、未改数据库、未提审或正式发布。

唯一下一任务：完成并推送干净检查点后，报告精确 SHA；取得用户对该检查点的当次上传确认，再在同一新体验版上分别验证 3.17.2 异常实例恢复及 3.17.3 正常实例外观不变。
