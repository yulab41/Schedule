# 护士照片排班导入与多人补录

## 授权和范围

- 用户批准正式护士群2026-09-07至20的139条排班；D5映射电脑，A编号映射A，D1/D2映射D；休假、特殊班次、指定跳过成员及群外人员不导入。照片、名单、差异及操作数据仅存ignored runtime/audit/nurse-import-20260911。
- 用户另行明确批准多人补录、静默导入支持、应用修改和生产部署。无新增成员、班次或数据库迁移，无小程序上传。

## 根因和行为变化

- 引入点27992c758：批量契约以岗位/日期去重，服务以岗位/日期取第一槽，原设计不支持同日多人；git log -S与git blame及生产编译代码一致。
- 新增可选matchByMember=true：按岗位/日期/有效成员查找（实际人员为空时使用计划人员）；多个匹配拒绝；新成员追加独立槽位，事务群组/期间锁串行保护。默认旧模式及旧幂等指纹保持，既有医生补录行为不变。
- 手排应用新增notifyMembers=false：正常权限、预览、发布、统计、事件继续；不生成成功或冲突通知及投递。默认通知行为不变，静默选择进入幂等指纹及应用事件。
- 非重构：以上为显式新增模式，不改变旧调用的接收者、异步错误、空值及次数语义。无依赖或数据库结构变更。

## 验证

- 旧契约回归1失败/2通过；旧应用MySQL新增两用例均400失败。恢复修改后契约与路由10通过，真实MySQL既往补录12及手排19共31通过。
- 额外真实MySQL组合事务1通过：静默手排和同日两人历史补录共4条，在校验故意失败时一并回滚至0，通知0。共32个不同MySQL用例通过。
- pnpm verify通过：格式/lint/build/typecheck、Mini1122通过/15跳过、根1241通过/424跳过、依赖保护81通过；跳过的真实MySQL用例不计为全量通过，32项专项已单独运行。
- 运行/浏览器验证：pnpm smoke:browser（SMOKE_BASE_URL=http://127.0.0.1:4173）登录/管理员/成员/访客/访问记录通过，无浏览器错误；pnpm smoke:check-core通过。
- 浏览器环境准备：5173被系统拒绝，改用4173；Web显式开发登录；本地合成local-admin临时校准平台标记后恢复原值，不改生产账号。

## 正式导入保护

- 写入前实时核对群组、规则、成员、班次及目标日期为空；现有59条9月1—6日数据必须保持业务字段一致。
- 以执行时北京时间08:00业务日期分流，正常手排静默发布，历史按成员模式31条分批；所有服务在同一外层事务运行，保留权限及幂等事件。
- 事务内确认139条逐项一致、原数据保留、通知和投递未增加，业务日期未跨界后提交；先进行同代码回滚演练，再提交并独立回读。
- 生产备份542b8bcb-9796-4cb2-92b0-25d740229594，54表、105228472字节，文件SHA-256与登记一致；即时部署前live为b618d938，正式部署仍须再次核对。
- 当前阶段：代码、部署与139条正式导入已完成；最终结果见下文，不宣称真机验收。

## 生产演练补充

- c4a9b67f已推送部署，完整生产verifier通过，schema57不变。演练全部回滚，无正式排班写入。
- 发现已确认照片中一组NP次日11:00结束、A次日08:00开始的3小时重叠；按用户已确认的原始班次保留，不更改时间。只准许该已识别重叠，任何其他冲突继续阻止录入。
- 手排正常只填计划人员、实际人员为空，回读应使用实际优先/计划回退。由此发现c4a9b67f新增成员模式未回退计划人员会重复插入：RED产生3条而非2条，修复后真实MySQL12项通过。
- 补充修复仅API成员查找及其回归测试；API build、定向lint/格式、smoke:check-core通过，复用未变更Web/契约的完整浏览器与构建证据。检查点fix(schedules): match planned members during backfill，部署后继续同一139条事务导入。

## 最终交付

- 最终应用a90e3b0a2b1db4eb252eb73e3940964bbf83813d已推送部署，完整verifier通过，schema57不变。部署即时前驱c4a9b67f；备份f2cca68a-7547-4b16-8d0a-841019f55cce，54表、105250592字节，SHA-256为2efcb5a035628f03ea6b5544d4c11ca1bc26709f074671eff359b10e71fee44a，已读取真实文件核验。
- 2026-09-12 08:08:42（北京时间）单事务提交139条，按08:00业务日分流为历史54、当前/未来85；提交前同代码演练完整回滚并验证。原有287条有效排班逐条业务字段不变，含9月1—6日59条。
- 08:09独立生产回读：139条逐项一致、重复0，成员/岗位/班次配置不变。A55、D28、电脑14、P14、N14、NP14；指定跳过记录未新增，群外人员未新增。
- 通知和投递均新增0。保留原照片已确认的一处NP/A跨日3小时重叠；不以数据导入替代排班合理性或真机验收。
- 两次打包均复用85个生产依赖、下载0；workspace无安装。补充修复复用不受影响的验证，仅重跑相关MySQL及API门禁。
- 个人名单、冻结输入、原记录快照、回滚演练、提交收据、独立校验均位于ignored runtime/audit/nurse-import-20260911；不提交个人班表、凭据或生产数据库。
- 收口仅文档，不重复生产部署/导入；下一步用户刷新护士群日历查看9月7—20日。
