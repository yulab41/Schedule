# Feedback6 平台账号与统一手机号

## 行为与数据

- 基线528722f4，独占general-1/codex/feedback6-accounts；Acquire→ReuseOnly→root Bootstrap，无依赖安装。保留a6586326微信换绑及网页微信登录退役。
- 平台Web/Mini显示姓名、账号、统一手机号、密码状态及密码/微信/待绑定成员/历史成员身份/未完成账号分类，支持修改姓名、手机号和设置新密码。分类描述现存关联，不冒认为建号事件或测试身份。
- 新增`GET /platform-admin/users/details`、平台profile/password写接口。旧账号状态接口及strict契约不变；平台权限仍由服务端校验，普通群管理员不能借此修改他人手机号。
- users保存统一手机号及专属变更时间。各群手机镜像同步；短号和显式撤回公开状态各自保留。日历、联系人、员工编号匹配、公开设置读取统一号；新群无联系人行也可显示/撤回，逻辑version0可首次提交。
- 迁移0055按未删除联系方式updatedAt倒序、contact ID升序选择非空号码，用户已接受updatedAt并非手机号专属更新时间的局限。原值保存至`mobile_phone_before_account_sync`；保留原updatedAt、短号、确认及撤回字段，只有实际号码变化行增加内部version，拒绝旧页面写回旧号码。
- 运行时明确改号沿用取消号码确认的规则；各群撤回状态不互相复制。认领/合并保留最近账号号码与时间，较新的显式清空也参与选择；成员退出创建历史身份时复制当时号码。
- 密码采用既有随机盐scrypt；仅请求内存有新密码，幂等指纹用服务端密钥HMAC，不返回/记录密码或hash。重置增加authVersion使旧会话失效；本人重置返回登录。输入成功/关闭/隐藏/销毁后清空，选中账号消失及版本变化处理重试状态。

## 来源排查（根任务生产只读证据）

- 本地连接说明已恢复，根任务只读核验live=a6586326：35账号中11个无用户名，8个仅历史成员身份、2个停用、1个未关联成员。
- 8个历史身份都有排班历史，不能删除；创建时间不证明是退出自动占位。2个停用账号仍有旧认证定位，其中1个仍有关联业务；未关联账号与现有密码账号同名，也不能据此认定测试。
- 11个均无测试姓名或UID前缀证据，没有删除任何账号。正常建号入口包括预设未绑定成员、历史占位、微信/资料注册；旧网页登录历史不代表恢复已退役入口。
- 本子任务未连接生产、部署、上传或执行生产数据写入。上述聚合来自根任务核验，不保存姓名、手机号或完整账号ID。

## 验证与修正证据

- 平台新API先红：2失败/11通过（旧接口404），实现后13通过。后续真实MySQL最终49通过：平台18、成员路由13、微信解绑18，日志`runtime/audit/feedback6/accounts-integration-close.log`。
- 另已通过群权限10、邀请8、日历16；邀请增加统一号码合并断言。最终49含旧token实际拒绝、迁移最新/同时间排序/原值保留/旧页面409、新群version0撤回、明确清空不复活、成员退出复制、本人/平台并发修改。
- 真实死锁barrier初次超时，原因是测试工厂固定单连接；本文件改用隔离schedule_test专用4连接池，未改全局测试工厂。定向4项（迁移/死锁/清空/并发）通过，随后平台整文件18通过。原超时及修正日志均保留。
- 迁移旧写回用例一度404，查明createGroup自动添加开发管理员，夹具误选管理员身份；限定cloudbase-member后原409断言通过。原群权限用例禁止手机号跨群复制与批准新行为冲突，改为验证统一号码且两个群的撤回独立，没有放松权限断言。
- 新顶层账户/联系人/邀请事务采用已有三次内死锁重试；原微信循环逐字移至通用函数、保留export alias。接收者、返回Promise、仅ER_LOCK_DEADLOCK捕获范围、次数和失败传播不变；原微信网关仅交换一次及回滚用例全部通过，外部动作不置于重试事务内。
- 共享客户端/契约及Web等价7项、Mini账号/页面边界25项通过。API/Web/Mini类型、API/Web构建、定向ESLint通过；没有重复运行全量verify。
- 运行/浏览器验证：pnpm smoke:browser 原流程完整通过，登录/管理员/成员/访客/访问记录无浏览器错误。80ms触摸采样的独立修复与失败证据见feedback6-smoke.md，提交5e420f2c。
- 账号专项真实浏览器完成姓名/手机号、用户名、新密码保存，密码清空；390/320×16/20四组窗口无溢出、无pageerror。仅用本地合成账号，临时管理员标记已恢复、测试账号按精确ID清理。首次QA未设置管理员标记导致正常403，修正夹具后通过，未绕过产品权限。
- Mini verify与package audit通过：主包1752032、总包5168992字节；Worklet2/2、确定性通过。仍有既有主包1.5M内部提示及矩阵节点1445/1505提示。本轮没有重新构建干净基线，故不宣称同口径包体改善。

## 检查点与下一步

- 账号检查点标识：`feat(accounts): manage profiles and synchronize account phone numbers`；仅创建本地提交交根任务review，不push、部署或上传。
- 最终发布门禁：现有ecs-schema-compatibility仅审定schema53/54，0055后的打包会主动阻断；轮转后续迁移完成后由根任务一起审定新的兼容范围并测试，不能沿用54或绕过检查。
- 状态：已完成自动化与浏览器验证，待最终同版本小米14复核。Web截图和Node校验不等于Skyline或实体设备验收。
- 根任务下一单元是删除自动轮转及请假自动补位，之后集成全日期数据审计和最终发布验证；本租约保持待review，服务进程交接后停止。
