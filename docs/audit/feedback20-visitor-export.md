# Feedback20：访客加载、返回登录与二维码长按

## 结论与引入点

- CSV 真机提示“微信下载域名校验失败”发生在 `wx.downloadFile` 发出业务请求前。production URL 回归确认地址为 `https://hosp.schedule.eylinhome.top/api/groups/.../exports/.../download`；这是公众平台 `downloadFile` 合法域名配置问题，不是账号权限、CSV内容或导出API错误。代码继续使用Bearer请求头，不用URL token绕过。
- 匿名访客页从 `890efd8b` 起在每次月/周/列表切换时清空页面、重新resolve访客码并读取当前及相邻月份；成员页复用内存月窗。频繁“正在读取排班”是现存代码差异，不仅是正式版103较旧。
- 相册保存链路来自 `9bae5beb`。按本次要求删除按钮、权限、临时文件及相册API；保留 `.121` 已交付的点击预览、轮换后自动读取和PNG/JPEG识别，并启用长按保存/转发。

## 修改与安全边界

- 访客Page仅在内存保存群组、月份、节假日及进行中请求；视图切换复用月窗，缺当前月才全页加载，相邻月后台补齐，重复请求合并。
- serial与context generation共同隔离迟到响应；hide/unload、显式重试、403/404/410/访客码失效清空访客内存、电话、事件和筛选。未写Storage，未改变成员缓存、共享日历组件、API或数据库。
- “返回登录”描边按钮仅在匿名访客页视图控制行，位于月/周/列表与筛选之间；复用`ui-chevron-left.svg`，按压位移和reduced-motion均为访客页局部样式。

## 验证与交付

- 本任务原始RED3后定向51通过；合并最新累计体验版`.121`后，访客/二维码/导出/血缘定向76项中71通过、真实转换5项在刷新production产物后通过；presentation-core导出7项通过。
- 合并后完整`pnpm verify`通过：Mini1159/16跳过、根1254/439跳过、依赖保护81；icon parity、Mini verify、Worklet2/2、确定性、包体和smoke:check-core通过。最终production包主包1,709,746、总包4,549,449字节；相对`.121`记录的4,551,833字节减少2,384，保留既有主包和矩阵预警。
- 原始检查点`325f82ea`已推送。首次`.122`上传在调用微信前因`.121`不是祖先被血缘门禁拒绝；没有上传，`.122`按不可变规则视为已占用，不删除或复用。后续候选必须合并`.121`并使用新版本。
- 不部署API/Web、不改数据库、不提审/正式发布、不退役旧版。公众平台downloadFile域名仍需外部配置并由小米14验证。
