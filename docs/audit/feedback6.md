# Feedback6：日历、排班、账号与通讯录

## 已批准边界

1. 月/周/列表按北京时间08:00统一业务日期，历史浏览不被抢走。
2. 全日期及2026-07-08审计事件、补录及工作流引用，缺事件不能据此删除真实排班。
3. 详情事件记录与换/加标签同排收紧。
4. 平台显示姓名、统一账号手机号及密码状态，允许编辑；密码不保存/显示明文。多值旧手机号按联系方式updatedAt倒序、ID稳定排序选取并保留迁移前值。仅清理确证无业务引用的测试账号。
5. 手排复用公共自绘日期选择器，默认max(业务今日,当前岗位有效排班末日+1)，不补中间缺口。
6. 班种颜色等非可用性变化不再使模板失效；真正失效格可替换/清除。
7. 删除自动轮转、顺序/规则设置及专用前后端表/代码，保留手动模板、发布、历史和内部并发校验。请假批准只清本人冲突班次，无自动补位；撤销不覆盖后续人工修改。
8. 日历展示采用当前班种颜色，历史班种缺失时回退快照。
9. 补录改用共用月历，岗位独立，按钮文字定宽，说明单行。
10. 月历显示原姓名黑色划线与新姓名红字；跨月/岗位草稿最多31条，固定确认/清空，保留已完成历史。
11. 首搜按readiness/认证/SQL/传输/渲染计时定位，只优化实际慢路径，不猜测全量下载或定时缓存清空。

## 基线与引入点

- 基线4027d0fd，承接a6586326身份修复；健康general-1独占。未触碰canonical原有未跟踪文件。
- pnpm verify初始在wechat-diagnostics.ts既有格式问题停止，原始证据runtime/audit/feedback6/baseline.log；尚无全通过结论。
- 日历自然日及模块常量来自ad4cfb2c；详情变更判定d9296df0；旧补录无事件927241c1；颜色快照读取ab250646。
- 模板默认today来自591ccff6；配置版本失效判定65122746，无条件递增0bb586548。通过git log -S/blame只读确认。
- 首搜旧.94证据SQL4183ms，.95记录启用candidate；最新.96后无本轮生产/真机计时，不能视为同版本证据。

## 验证与未决外部事项

- 新日期回归6例：旧实现4失败/2通过，修复后6通过；其余定向回归正在执行。
- 生产连接说明runtime/local/production-operator.md在规划时缺失，用户选择实施时恢复；此后已由根恢复连接并完成全日期只读审计，未删除生产数据；详见feedback6-data-audit.md。
- 新版可用后要求旧Mini升级，不保留永久后端轮转兼容层。实际上传/生产变更按当次发布门禁执行。
- 第一检查点实现1/3/5/6/8/9/10及2的新写入事件修复、11的readiness等待优化；4账号统一号码已完成（98aa0910），7轮转删除已实现并验证；生产数据只读审计已完成，真机性能仍未测量。

## 第一检查点验证

- 标识：fix(scheduling): align duty dates and compact backfill editing。所有变更均属于已批准feedback6；公共日期选择器只迁移注册路径和相对引用，接收者/事件/滚轮实现保持，行为变更另外由日期、模板和补录用例覆盖。
- 初始verify的格式问题修正后，format/lint/build/typecheck/icon通过。全量Mini941通过、6个旧源码路径/循环变量断言失败；更新这3文件的断言至相同组件的新路径，42项定向全部通过。没有削弱交互断言。
- root测试Node门禁通过，Vitest1185通过/387条件skip；唯一旧“只看变更”预期与批准的新语义冲突，新增历史补录语义说明并定向通过current-month-calendar整文件。保留初始失败，不改写为一次全量verify成功。
- 真实隔离MySQL三文件36项通过，64.62秒：日历颜色和新默认日期16项、模板9项、补录事件11项。旧日期夹具固定在2026-08窗口；过期幂等夹具改用同一冻结时钟而非数据库NOW，产品时间校验未改。
- Mini日期/持久定位50项，picker原交互20项，补录/手动16项及后续销毁保护定向通过；readiness与Server-Timing33项通过，readiness新例先失败再通过。
- 运行/浏览器验证：pnpm smoke:browser原脚本在本槽位API/Web通过，覆盖登录/管理员/成员/访客与访问记录，无浏览器错误。沿用已有本地配置内存适配器，合成管理员标记前后恢复。首轮缺VITE_AUTH_DEV_MODE、并行重测期触摸反馈失败均保留，环境正确且空闲复测通过；未改断言。
- Mini verify通过，Worklet2→2、确定性/源码/包体门禁通过；最后一次完整校验总包5114769字节，主包仍有既有1.5M内部提示。后续仅增加销毁页面拒绝重载一行，补录定向及production构建再次通过；最终发布时重新冻结包体。
- 390/320×16/20字号补录CSS桌面代理4项通过，证据runtime/audit/feedback6/，不代表Skyline或小米14。当前状态为自动化已完成、待实体设备复核；没有生产部署或体验上传。

## 下一集成单元

根任务review本地轮转检查点并完成schema56前向不兼容保护、旧客户端强制升级和最终候选门禁。账号与轮转实现分别见feedback6-accounts.md、feedback6-rotation.md，生产聚合事实见feedback6-data-audit.md。
