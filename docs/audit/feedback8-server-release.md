# feedback8 服务端交付

- 应用：`8e68a480ba7608025a9af262e7d6fe5369071161`，`fix: repair scheduling previews, invitations and WeChat reminders`。
- main与GitHub正常快进推送完成；用户原有10项未跟踪内容保留。账户文档检查点57e6330a同时进入main。
- 依照用户根AGENTS对包含API修改检查点的自动生产部署指令执行；这不构成微信体验版上传批准。尚未分配新版本号、调用微信上传或修改版本允许列表。

## 发布证据

- 2026-09-10即时读取服务器应用`36fae3d145982d793c1dee243a6eb12867962fb6`，确认其保留包存在，将其锁定为本次回滚候选；发布锁内再次核对live未变后执行。
- 独占general-1候选冻结为干净detached 8e68a480；worktree safety通过，依赖指纹`aef084f57bf1321a10e60130f912fe4ef35127421f368cd461c490b7ef7ec527`。工作区无安装；官方ECS打包器对独立发布运行包执行离线导出，复用85项、下载0项。
- 部署前新数据库备份：`d5dac482-6607-434e-94cc-8e1711738942`。包含清理后的业务状态，文件102249848字节、53表、239615行，SHA-256 `3410f99f50ff001dbf0489691644dde947ef23ab29e17f2602b54c9fd4cae62e`。备份登记、时效、实际文件长度及hash一致；既有保留策略淘汰1份旧备份。
- 本地包与服务器上传文件分别核验：dist `2ce5d7386ebf84e431012a6316bfd6337d7072c11204beb14ed6ca97d3eabe11`；API flat `8c97a9043a8d351549b9845530cc28619ed38785fb90c2a43e4499d6494f20d7`。更新/验证脚本hash与提交匹配，Bash语法通过。
- 通过既有ecs-update发布流程完成迁移检查、容器重建及健康检查。无新增迁移，schema仍56；重建后前三次健康检查为暂时502，后续正常并完成7/7。原隐私保留任务本次删除0行。
- 最终安装的ecs-verify通过：产物、控制面、容器、迁移、域名健康和访问边界检查完成。按脚本原逻辑未进行未配置公网IP的主动探测。
- 既有版本允许列表verify通过；本轮没有追加或删除版本。

## 功能和数据核验

- 在已部署API容器中加载实际编译模块，以注入的模拟网关执行四字段映射，结果为`thing6/character_string7/thing9/thing8`及正确值；没有调用微信发送API，真实发送0。
- 生产新连接只读核查：32账户（24已设置、8历史待设置）、4011排班；计划/实际排班的缺失成员引用均0。账户清理结果保持，未复制本地数据库或测试资料到生产。
- 本地自动化、浏览器与包体结果见feedback8.md；当前Docker启动故障不被当作新MySQL通过证据。生产模拟网关检查不等于微信真实收信。

## 剩余交付

服务器修复已生效，但手机端视觉、草稿decoder及邀请卡片需新体验版。等待用户授权当前干净提交上传及新版放行；之后分配版本、上传、只追加新版并保留旧版，再由用户验证小米14、好友/群聊接收及本人通知。

本次收口文档提交不再次触发服务器备份、部署或发布标识同步；生产应用保持8e68a480，后续文档SHA仅记录已交付应用。候选租约已正常释放，随后独占复用同槽更新这些记录，未创建冷工作树。
