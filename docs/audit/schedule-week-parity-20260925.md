# 共用周历渲染与动画：2026-09-25

## 范围与归因

- 基线 `f8010455`；独占 warm `general-4`，`DEPENDENCY_MODE=REUSE_ONLY`，安装次数 0。已有 `.196@aef86299` 不是本轮代码。
- `git log -S 'week-day-grid'` 将首页周格定位到 `ad4cfb2c`；`git log -S 'previewWeekPanels'` 将独立预览周格定位到 `aef86299`。`git blame` 确认首页周格数据成形于 `733e3af67`，后由 `e94a54caf` 扩充。补录与预览原先把七天塞进月格，失去分组、换行和首页样式。
- 只改小程序内部组件和视图模型，不改服务端接口、持久化结构或依赖。

## 实现与语义检查

- 新共用 `calendar-week-panel` 展示日期、节假日、补班日、班种浅色分组、姓名与草稿标记；首页与 `calendar-month` 的周模式调用同一组件。月模式保持原来的 `calendar-cell`。
- 共用 `calendar-week-model` 按护士既定电脑、D、A、P、N、NP 排序，其他群按配置顺序；按班种 ID 分组，颜色与浅色背景跟随班种。高度按姓名换行与标记计算。
- 手排预览传入群组名与班种顺序，已有排班的纯对照分组灰显，混合组中旧人员灰字，本次预览保留班种颜色。补录原班与拟补录通过标记区分。
- 首页点击从原生节点事件变为组件 `select` 事件；日期值仍原样传给 `selectBusinessDate`。月格日期事件路径未变。补录跨周定位先读取目标周所涉月份，再用已有 `calendar-period-pager` 单次过渡；读取失败保留显错和禁用。刷新有缓存月份时保留已加载格，失败后清除缓存并显错。

## 验证

- 旧实现上新增护士排序/分组/高度回归先失败，修改后定向 43/43 通过；首页运行时与今日标记 41/41；迁移后静态断言定向 29/29。
- `pnpm --filter @schedule/miniprogram verify`：TypeScript 与 production 构建通过，总包 4,815,725 B；主包 1,832,928 B，超过项目内部 1.5 MB 提示阈值。手排矩阵 1510 节点已有 best-effort 提示。
- 开发者工具 `check_wechatide_status` 兼容且已登录；共用周格、月格、首页 WXML 和共用周格 WXSS 局部编译通过。模拟器打开补录路由并截图，停在“正在加载既往排班”；Console `grep -i error` 为空。截图仅保存在 ignored `runtime/audit/`。局部编译和加载画面不等于实体设备表现；当前工具未取得含真实排班的补录与三类预览截图，连续滑动、320px、弹窗滚动及小米 14 原生动画暂未验证。
- 最终 `pnpm verify` 通过：Mini 182 文件通过/2 跳过，1274 用例通过/17 跳过；根 276 文件通过/36 跳过，1306 用例通过/451 跳过。源码、包体和 `git diff --check` 通过。首次总门禁只因 `docs/project-status.md` 超过 40 KiB 而失败；将旧 Feedback9 流水留在 Git/审计记录后，文档门禁与完整重跑均通过。行为差异限上述周格迁移、排序分组、高度、灰显与跨周定位；无接口或数据库行为变化。

## 交付边界

本批只提交并推送 Mini 与文档检查点。未上传新体验版、未放行，也不部署生产。新构建须经独立体验版流程和同构建小米 14 证据后才能认定原生验收通过。
