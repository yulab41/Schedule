# Feedback12：108体验版十项反馈

- 用户确认108仍失败；实施基线5f79236f。任务覆盖二维码、导出、通知及护士日历，独占general-4/5，依赖复用，无安装。执行面只用Node/本地浏览器，未操作微信开发者工具或生产。
- 最终设计修正：状态无箭头，触控整个班种头部折叠；周历保留实色班种徽标并叠浅色分组背景；月历仅群组默认班种、无该班留空；保持原电话图标和无关UI。用户明确接受样张方向及上述修正。

## 十项跟踪

| 项  | 内容               | 当前结果                                                                     |
| --- | ------------------ | ---------------------------------------------------------------------------- |
| 1   | 二维码保存         | 原生保存分类与安全诊断、清理结果分离已实现；实际手机原因待新版诊断           |
| 2   | 导出白屏           | 初始化30秒截止和启动阶段诊断已实现；整页白屏尚未复现，不标修复完成           |
| 3   | 自动通知           | 双模板、业务队列及目标版本代码和本地验证完成；模板与群ID配置、生产链路待授权 |
| 4   | 默认班种           | 群组默认值读取、独立请求序号及无班留空；访客只读显示设置已接入               |
| 5   | 事件入口           | 多排班时状态按钮打开事件弹窗，原事件行隐藏，权限与电话隔离保留               |
| 6   | 详情顺序/状态/折叠 | 护士电脑-D-A-P-N-NP；分段状态、跨日、临时手动展开和下一边界计时              |
| 7   | 标识与姓名分行     | 月历徽标独立，姓名+固定人数；周历徽标位于人员上方                            |
| 8   | 周历完整显示       | 内容初始高度+原生布局实测高度，整页滚动，长姓名允许换行                      |
| 9   | 周历分组           | 班种原色徽标与约9%同色圆角背景，人员按槽位堆叠                               |
| 10  | 列表               | 同一护士排序与状态，电话图标不变、状态至电话至少12px                         |

## 状态与行为边界

- 北京时间；D/电脑08:00–12:00、14:30–17:30；A08:00–15:30；P15:30–22:00；N22:00–次日08:00；NP17:30–22:00、次日07:00–11:00。
- D/电脑午间与NP22:00–次日07:00听班均为休息中。区间含开始不含结束；最终结束后已下班。预设只作用于头颈外科护士群中明确映射的班种，不改原排班数据。
- 已下班/休息中默认折叠；同页人工选择保留，切日期/视图/群/主工作区及重新进入重置。后台清计时器，前台立即重算。
- 月历只取群组默认（用户最新要求优先于早期“回退下一个班种”选择），无偏好响应时不闪现其他班种，失败给出说明。其他月历筛选仍可缩小结果，不扩大到其他班种。
- 月历人数拆独立字段，避免窄屏姓名省略吞掉人数。周历保持原12px姓名字号；内容实测覆盖长姓名和尺寸变化。

## 引入点与验证

- 月历直接取dayAssignments[0]来自ad4cfb2c；周历112px固定高度来自50c6d1ed；详情优先时间排序及默认偏好遗漏已通过git log -S/blame定位。二维码/导出见feedback12-export-qr.md，通知见feedback12-notifications.md。
- 修改前Mini verify通过，Worklet2/2，主包1679972/总包4502799；已有主包1.5M内部提示和手排节点提示保留。日历定向基线49通过。
- 新日历回归RED21失败，初次GREEN45通过；用户修正严格默认班种后RED1/23通过，再实现严格筛选。状态/展开24项、运行时28项、事件7项及既有UI9项定向通过。
- 检查发现偏好与日历请求序号共用竞态，改为独立上下文；补慢响应翻周和隐藏页迟到测试。旧两项UI结构断言按用户批准行为更新，不降低权限/事件验证。
- 生产CSS+真实ViewModel桌面几何：390/320三视图无横向溢出、人数可见、周历无裁切、状态位于头部右侧、实色徽标和电话间距通过；脚本apps/miniprogram/scripts/feedback12-layout.mjs，证据ignored runtime/audit/feedback12/layout。
- icon:parity首次因general-5旧ui-tokens构建产物失败；官方root增量bootstrap重建变化生产包，无依赖安装；复测57图标/动效一致。未修改图标资产。
- 完整Mini首轮1146通过/15跳过/3失败；两项旧UI断言已按新需求调整，偏好运行时测试补充阶段断言后定向通过，最终全量Mini1160通过/15跳过，根1247通过/433跳过，依赖保护81通过。

## 完成边界

- 代码、自动化、Git推送、生产部署、体验上传、小米14原生验收分别报告。不补发历史通知，不主动真实发送。
- 本轮仍缺二维码原生分类证据、白屏启动阶段报告、实际业务模板ID/字段映射、目标群ID生产配置及最终同版本小米14证据。
- 交付更新：用户已授权上传及放行，109/a9d5a1e已完成，详见feedback12-trial-release.md。唯一下一任务为小米14同版本复核；生产诊断/部署与真实业务模板配置另行授权。

## 最终联合验证

- 完整format/lint/build通过；联合typecheck后精简月历偏好内存字段，最终Mini typecheck/build/确定性/包体通过。主包1702424、总包4529307，Worklet2/2；既有内部提示保留。
- 最终Mini1160通过/15跳过，根1247通过/433跳过，依赖保护81通过；433项包括未设置隔离数据库环境的集成用例，71项相关真实MySQL已专项运行，不把跳过算通过。
- 运行时测试原先未关闭前例页面，遗留请求污染后例wx；现每例关闭页面/使请求失效后卸载全局模拟。完整重跑通过。额外账户切换前台清理回归RED1后GREEN，避免状态刷新带出旧账户日历。
- 新增非法体验群ID配置RED2后GREEN25，禁止无声回退正式版。业务模板配置仍待实际ID/映射，未替用户编造。
- 运行/浏览器验证：pnpm smoke:browser通过完整本地流程，pnpm smoke:check-core通过；local-admin合成夹具原标记已回读恢复，3105/4175自建服务已停止。实施阶段生产未连接；后续上传与版本放行见feedback12-trial-release.md。
- 证据保存在本槽ignored runtime/audit/feedback12；未将手机图、二维码、凭据或业务明细提交Git。
