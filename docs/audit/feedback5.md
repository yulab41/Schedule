# Feedback5 集成验收记录

2026-09-07；代码验证基线为干净 `036d376f`，应用改动来自0768eef4、d3d6e2c6、040f03b7。本轮仅修改、测试、推送及获准的生产只读检查；不上传、不部署、不切换配置、不发送真实通知。

## 反馈与状态

| 编号   | 级别 | 原因与处理                                                      | 状态与验证                                                     |
| ------ | ---- | --------------------------------------------------------------- | -------------------------------------------------------------- |
| FB5-01 | P3   | 筛选图标残留字面量`>`，删除模板字符，保留图标与动画             | 高置信，回归与桌面视觉通过                                     |
| FB5-02 | P2   | 首搜4.423秒中主SQL4.183秒；线上仍为legacy，非手机下载全量联系人 | 瓶颈证据明确；候选配置待授权，速度改善未验证                   |
| FB5-03 | P3   | 账号卡与末行padding叠加，缩小底部留白                           | 高置信，保留触区，390/320大字号代理通过                        |
| FB5-04 | P3   | 特殊日期长说明被省略，改“本月周末/节假”并允许换行               | 高置信，统计口径不变                                           |
| FB5-05 | P3   | 下一班负字距及紧密连接，恢复正常字距并分隔时间                  | 高置信，时区及跨日算法不变                                     |
| FB5-06 | P3   | 部分保存反馈仍为常驻块，统一ui-toast与唯一共享两秒timer         | 高置信，直接Page/组件、重复、hide/unload、账号/群组ABA回归通过 |
| FB5-07 | P2   | 原来缺少订阅到投递各阶段观测，新增仅本人受控诊断与主动测试      | 自动验证通过；平台模板字段和真实手机送达未验证                 |
| FB5-08 | P3   | 原趋势只有四个月且只取当年，改含本月滚动12月、最多取两年        | 跨年/十二月/缺失/零值回归通过，Web不变                         |

文件位置、引入提交、设计取舍及独立证据分别见[UI记录](feedback5-ui.md)、[群组反馈](feedback5-group-feedback.md)、[微信诊断](feedback5-notifications.md)、[生产查询检查](feedback5-directory-inspection.md)。主要新增风险为年度多一个读取请求及主动测试消耗一次订阅；缺失数据不伪装为零，测试需要用户点击且不会自动重发。

## 集成验证

- `pnpm verify` 最终通过，324.83秒：format、lint、全构建/typecheck、icon parity、Mini926通过/15条件跳过、Node81通过、root1186通过/372条件跳过。后者的条件跳过不代表相应数据库集成已运行。
- 本人诊断另经既有本地MySQL及官方wrapper运行5项真实数据库测试通过，外部微信网关为stub；验证同键/异键并发、限频、持久unknown重放、过期/权限/无效群组/mock拒绝。没有发送真实微信通知。
- `pnpm miniprogram:verify`、`pnpm miniprogram:package-audit`、`pnpm smoke:check-core`通过。源码/输出Worklet均2，确定性通过；未触及Web核心链路，无需启动浏览器业务冒烟。
- 独立桌面布局证据：个人页390/320及大字号；群组/通知共用胶囊28组合；诊断按钮390/320×16/20px字体4组合。均为CSS/Node代理，不代表Skyline或实体机。
- 集成中先捕获新测试wx全局声明lint错误，显式使用globalThis后11项回归通过；再捕获旧toast清单要求两套timer的过期断言，改为严格只允许共享timer后24项回归通过。保留失败日志，未改变业务代码来迎合测试。
- 最终门禁日志保留在独占general-3的ignored `runtime/audit/feedback5-final/`；分组证据路径见各记录。收口仅文档变动，复用相同应用输入的验证证据。

## 包体

同一production静态构建口径：主包1693649→1702602（+8953 B），总包5062991→5096194（+33203 B）；无新依赖、无数据库迁移。构建输入332→333文件；磁盘build-manifest另计，不混用口径。

| 包           | 2ed5035c基线B |   最终B |  变化B |
| ------------ | ------------: | ------: | -----: |
| main         |       1693649 | 1702602 |  +8953 |
| scheduling   |        415198 |  415158 |    -40 |
| organization |       1016750 | 1017334 |   +584 |
| workflows    |        822058 |  821981 |    -77 |
| insights     |       1047357 | 1056585 |  +9228 |
| diagnostics  |         67979 |   82534 | +14555 |

干净基线在general-1独占重测，构建1826ms/包审767ms，证据为ignored `runtime/audit/feedback5-baseline/measurement.json`；未重复安装或测试。仍有原有主包1.5M内部提示和矩阵节点1445/1506提示，正式包体门禁通过。

最终最大五个文件为workbench/index.js217300、scheduling/pages/manual/index.js182307、workflows/pages/swap/index.js180793、organization/components/directory-panel/directory-panel-controller.js180515、workflows/components/workflow-swap-panel/index.js180491字节。均位于生成dist，不手工修改。

同五个基线文件分别为217208、182327、180812、180480、180511字节；最后两者的大小排名在修改后交换。

## 发布边界与唯一下一步

- 本轮只读复核的生产版本为bfd1fbbd，已上传旧体验版为.94。新代码未上线，不能用旧版反馈判断新功能。
- 候选查询工具及索引已验证，工具hash与源码相同；启用candidate会重建API，不能把只读检查授权当成配置变更授权。
- 下一步仅为取得最终提交的部署、candidate切换及体验版上传授权，届时重新读取live、备份、冻结候选并通过官方版本分配/上传/允许列表流程；不提前分配体验版本，不提审或正式发布。
- 新体验版需由用户验证冷启动首搜、无关词搜索、八项界面与微信诊断。只有用户按当前版本确认手机收到，才记录实体机送达事实；不提前承诺候选查询根除所有长尾。
