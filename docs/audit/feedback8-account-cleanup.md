# feedback8：确认范围内的账户清理结果

## 授权与范围

2026-09-10，用户取消原先删除全部11个待设置账户的方案，改为只筛选“重复姓名，或从未出现在历史排班中的待设置账户”。历史匹配同时检查姓名快照及成员关联，避免因账户标识变化误判未排班。

新范围为3个账户：2个重复姓名的待设置账户、1个没有值班人员排班但有历史操作记录的账户。向用户列明必要姓名/短标识和关联影响，收到“确认删除”后执行。24个已设置账户及8个唯一姓名、有关联历史排班的待设置账户均保留。

本次仅为账户数据清理，不部署应用、不变更版本允许列表、不上传体验版、不发送通知。执行前与结束后生产应用均为`36fae3d145982d793c1dee243a6eb12867962fb6`。

## 备份与执行约束

- 已验证安装的备份控制脚本与控制面manifest匹配。
- 备份：`e03aba70-d331-48f0-a865-e383b625dd15`。
- 文件大小：102411900字节，53张表，239509行；实际文件SHA-256：`92bc9bdb6c06ca2f8be44d62bd15512b99ce3a4cabfe710f03d4270354d3a988`，与备份登记一致。既有备份任务同时按原保留策略淘汰1份旧归档。
- 初次校验使用客户端解析的时间戳，因时区差异被拦截；改由数据库计算备份年龄，元数据与实际文件校验通过后才进行账户操作。未放宽文件hash或范围约束。
- 固定3个候选，候选版本指纹`919ed95051acefafd09d08f823f315c5b613960af8a30fd11afc887bfe6daa40`；总账户、密码状态、管理员/微信身份、直接引用及外键子记录均在事务内复核。出现额外引用或数量变化即回滚，不关闭外键。
- 清理使用串行化事务、行锁及独占操作锁，先做同范围回滚演练，再正式提交。19张涉及或保留的表按预期删除/置空结果逐行摘要比对；对修改行只排除数据库自动更新时间。

## 实际变更

| 处理 | 数量 |
|---|---:|
| 删除账户及个人资料 | 各3 |
| 删除群组成员记录 | 2，其中1个有效身份 |
| 删除请假、成员认领申请 | 各1 |
| 删除通知 | 15 |
| 删除通知偏好、网页推送订阅、群组码尝试记录 | 各1 |
| 删除操作去重记录 | 16 |
| 保留审计记录，解除操作者关联 | 1 |
| 保留事件，解除发起人／操作者关联 | 各58处；不能相加当作116条不同事件 |
| 保留排班，解除补录操作者关联 | 7 |
| 解除换班、加扣班的审批人关联 | 分别3、1 |

删除前还将1处请假审批人和1处成员认领决定人置空；对应被删记录不额外计为保留记录。没有删除排班、事件或排班月份，没有迁移到同名已设置账户，也没有按姓名批量删除其他记录。历史JSON与姓名文本保持原样，仅处理已确认的实际关联字段。

## 验证结果

- 回滚演练后账户恢复35/24/11，正式提交后32/24/8；生产新连接只读核查再次确认。
- 3个目标账户均不存在；同名已设置账户仍为1个。
- 所有4011条排班均保留，所有排班的计划／实际成员标识、姓名和业务内容与执行前一致；只有确认的7处补录操作者关联置空。
- 原8个历史待设置账户涉及的1002条历史排班及成员归属保留。其余账户及密码、资料未改变。
- 计划／实际排班的缺失成员引用均为0；缺失账户的群组成员引用为0。
- 安装的生产verifier校验hash后执行，最终`[verify] complete`。域名健康、产物、容器和迁移检查通过；没有提供公网IP，脚本按原逻辑跳过该主动探测。
- 本地MySQL测试未完成：本机Docker未运行，启动又被已有Inference socket错误阻挡；没有重置Docker或安装依赖。不能将编写的6项本地测试记为通过。本次实际验证来自同范围事务回滚演练、19表比对及提交后生产只读核查。
- 首次演练在写入前因按`id`排序不适用于群组码尝试表而失败并回滚；改为读取数据库实际主键，后续完整演练通过。

## 后续

账户清理完成，禁止再次执行这组删除。回滚需要上述备份和单独授权；不将整个数据库恢复当作普通代码回滚。后续应用交付的当前状态见project-status.md及feedback8-server-release.md，不依据本次清理时的旧状态重复操作。
