# 头颈 DOCX 发布基础版本导出计划

1. 以完成换班后 `actualMembershipId` 已交换、`plannedMembershipId` 未变的夹具建立失败回归，要求 Word 只采用计划成员。
2. 将头颈 DOCX 成员归一边界改为只读取 `plannedMembershipId`，保留列优先、横线、请假下标、二值和三值视觉逻辑。
3. 完成定向、全量和 Word 视觉验证，形成累计干净检查点并部署生产。
4. 生产操作前只读核对2026年9月8日、9日一值基础/实际记录、成员身份和换班设置；备份后通过服务层事务调整基础记录，随后以徐漫彬发起、许少伟接受并在需要时审批至 `completed`。
5. 回读基础人员、实际人员、换班事件与 Word 导出数据源；最后上传新体验版并以 add-only 白名单放行，保留旧版本。
