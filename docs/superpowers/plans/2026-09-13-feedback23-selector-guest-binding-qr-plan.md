# Feedback23 实施计划

## 1. 基线与失败回归

- 记录独占 warm 槽、SHA、依赖复用、Git 状态和相关引入提交。
- 增加导出选择器 320/390 边界测试，旧代码因缺少字段容器而失败。
- 增加访客/成员周视图结构等价测试，旧访客模板因未使用 `shiftGroups/tint` 而失败。
- 为访客电话同意门槛、公开事件字段和绑定二维码权限/冲突建立 API 集成回归。

## 2. Mini 局部修复

- 仅在导出页为岗位、成员选择器添加本地 `.selector-field` 包装，保持共享 `ui-selector` 不变。
- 仅在访客周模板改用现有 `shiftGroups`，同步成员页浅色分组结构，不修改成员页面或共享日历组件。
- 访客详情接入已同意电话与公开事件，保持失效、竞态和凭证隔离。

## 3. 一次性绑定二维码

- 扩展现有绑定票据上下文、权限和生成前未绑定检查；群主/管理员仅限本群成员，平台管理员可跨群生成。
- 后端生成正式/体验小程序码，响应返回二维码图和显示字段；原始票据只进入二维码 scene，数据库只存哈希。
- Mini 邀请页增加绑定二维码入口与人员凭证卡；扫码预览、确认登录沿用现有绑定页。
- 新票据撤销同目标旧 pending 票据；过期、已使用、离群、版本变化及身份冲突全部失败关闭。

## 4. 验证与交付

- 运行定向 Mini、contracts、client-core、API/MySQL 测试，再运行受影响构建、typecheck、lint、图标 parity、Worklet、包体、确定性和完整 verify。
- 更新审计与项目状态，逐行检查 diff，提交并快进推送 `origin/main`。
- 因用户本轮已授权，部署 API/迁移并执行生产备份与完整 verifier；冻结最终 clean SHA，动态分配体验版、上传并仅追加放行，保留旧版本。
- 报告自动化证据与小米14待验收项；不提交审核、不正式发布。
