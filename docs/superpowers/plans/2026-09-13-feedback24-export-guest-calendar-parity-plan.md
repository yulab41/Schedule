# Feedback24 实施计划

1. 建立导出弹层、文件类型选择器及访客pager/护士预设/缓存提交的失败回归。
2. 导出卡片局部允许溢出，文件类型切换为现有单选 `ui-selector`。
3. 增加匿名访客显示设置安全端点与client-core读取；访客应用群组默认视图，并传入护士预设和默认月历班种。
4. 访客周/列表使用0ms归中、提交锁和有界队列；持久缓存只作失败兜底并扩大内存预取窗口。
5. 运行定向及完整门禁，更新审计/状态，逐行检查后提交并推送。上传和生产部署等待新的当前消息授权。
