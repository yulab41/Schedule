# Web 性能摘要

测试使用 Windows 11、Node 24、MySQL 8.4 隔离容器和真实 Fastify/MySQL 连接；指标不含公网网络延迟。

## 结论

- 当前单群组、常规月历和工作流请求满足 Web 1.0 验收目标。
- 压测覆盖日历读取、排班生成、并发写入和事件查询；数据库索引和事务是主要约束。
- 上线前应使用接近生产的数据量复测，并观察 API 延迟、连接池、慢查询、CPU、内存和磁盘。

详细原始数据以测试脚本和 Git 历史为准，不在此文件复制逐请求结果。
