# Web 1.0 调试与验证

本文件只记录当前 Web 问题、根因、行为变化和验证结果；历史细节以 Git 提交为准。

## 规则

- 回归修复先用 `git log -S`、`git blame` 定位引入点，再补失败测试。
- 审计接收者绑定、异步错误、空值、调用次数、副作用和权限边界。
- Web 核心链路变更记录 `pnpm smoke:browser` 与 `pnpm smoke:check-core`。
- 状态使用：已实现待浏览器复核 → 已完成 → 待用户复核。

## 当前问题（2026-08-13）

- 反馈：成员页请求认领数据返回“请求的资源不存在”；通知设置显示“服务返回了无效资料，请稍后重试”。
- 引入点：`d117bb0` 保留 Web 认领调用，`8ab9184` 删除对应 API；`ef3d20c` 新增 `wechatNotificationsEnabled`，旧 API 响应因此无法通过严格契约。
- 修复：恢复群组码认领、成员匹配、申请审批/驳回/撤销 API 和集成回归覆盖；通知偏好 schema 对缺失新增字段默认 `true`，错误类型仍拒绝。
- 回归测试：已补充旧通知响应兼容测试，并恢复完整成员认领工作流测试；恢复的 API 文件与 `8ab9184^` 一致。
- 运行/浏览器验证：pnpm smoke:browser 已通过，覆盖管理员/成员/访客流程以及成员表、通知设置页面，未发现页面错误、控制台错误或请求失败。
- 运行/浏览器验证：pnpm smoke:check-core 已通过；`pnpm verify` 已通过（54 个测试文件、422 个测试通过；项目现有环境保护条件跳过 29 个测试文件/252 个测试）。
- 应用内浏览器本地复核：管理员成员页出现成员表，通知页出现“我的提醒”；本地成员模式正常，控制台仅有 Vite 正常连接日志。
- `git diff --check`：通过。
- ECS 发布验证：ECS 数据库备份成功；最终候选发布完成迁移、容器重建和 Nginx/API 健康等待；HTTP 入口 200、HTTPS SNI `/api/health` 200；发布目录、归档、lockfile、dist、迁移和 Compose/Nginx 哈希逐项一致；失败发布的上一版本文件备份仍保留。
- 公网应用内浏览器：管理员成员页成员表加载成功，通知页“我的提醒”加载成功，两类旧错误均不存在；公网成员账号无两类旧错误；控制台无异常。
- 状态：已完成，待用户复核。

## 后续

新问题只记录一条短条目：反馈、根因/引入点、修复、验证、状态。不要复制完整终端输出或逐轮历史。
