# Changelog

本项目的所有重要变更都会记录在此文件中，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-08-02

### 新增

- 账号与群组：CloudBase 用户名密码登录、管理员预置账号、首次登录完善真实姓名、群组创建、群组码自动认领、人员预建、角色与班种配置、长短号联系人。
- 排班核心：固定顺序自动排班（含多角色轮值）、草稿/发布/版本替换、手动排班模板（单次周期/循环到指定日期）、当前月日历、换班、加扣班。
- 流程与事件：请假（两种重排策略）、换班（自动接受/管理员审批）、加扣班（双方确认/管理员审批）、不可变事件中心与安全审计、站内通知与浏览器推送。
- 统计与导出：月度/年度/周末/节假日/加扣班统计、统计快照与重算校验、排班与统计 CSV 导出。
- 平台能力：法定节假日导入与确认、数据备份（AES-256-GCM 加密）与恢复演练、平台管理员（任务/备份/群组恢复/用户状态）、群组回收、并发版本保护与幂等重试。
- 体验：响应式桌面/移动布局、月/周/列表日历视图、PWA 可安装、离线日历缓存、无障碍与对比度规范。

### 修复

- CloudBase 网关鉴权：启用路由鉴权后业务请求不再循环提示“需要先登录”，健康检查保留匿名 `/api/health`。
- 事件型云函数必须使用 `SCF` 上游（`WEB_SCF` 会返回 400）。
- SPA history 刷新 404 通过静态托管错误码重定向回 `index.html`。
- web-push 在 esbuild CJS 打包下的加载方式改为懒加载默认导出。
- 函数代码上传改用直传 zip，避免 COS 上传 60 秒超时。

### 运维

- GitHub Actions 验证流水线（格式、Lint、严格类型、单元/集成测试、生产构建）与 CloudBase 开发环境自动部署。
- 部署、运维、备份恢复、监控、生产就绪与 ICP/DNS/HTTPS 运行手册（见 `docs/`）。
- Web 1.0 发布说明见 `docs/releases/web-1.0-acceptance.md`；当前发布环境采用“公网 + 单账号全局授权”妥协方案，正式化付费项延后（见 `docs/deployment/production-readiness.md`）。

[1.0.0]: https://github.com/yulab41/Schedule/releases/tag/v1.0.0
