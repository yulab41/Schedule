# Worktree and dependency evidence

For dependency fingerprints, health, reuse, and authorized frozen reconciliation, use only the
[dependency lifecycle](dependency-lifecycle.md). Lease ownership belongs to the
[multi-parallel workflow](multi-parallel-workflow.md); candidate output provenance belongs to
[worktree/bootstrap](worktree-and-bootstrap.md). This compatibility route defines no additional contract.
