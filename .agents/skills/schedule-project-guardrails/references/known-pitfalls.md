# Known-pitfall symptom index

Read only the row matching the observed symptom, then open the linked routed reference or existing fact source. Search by ID with:

```powershell
rg -n -C 2 '<pitfall-id>' .agents/skills/schedule-project-guardrails/references/known-pitfalls.md
```

| Pitfall ID                      | Symptom/search terms                                                                                             | First safe response and fact source                                                                                                                                                                                 |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `fresh-worktree-missing-dist`   | Clean worktree; Vitest collection or typecheck cannot resolve workspace package declarations/dist                | Build only the missing producer in the same worktree; never borrow main-tree output. See [worktree/bootstrap](worktree-and-bootstrap.md).                                                                           |
| `powershell-expected-git-one`   | Strict native-error throws on `git diff --quiet`, `merge-base --is-ancestor`, or another predicate returning `1` | Capture the documented exit codes explicitly and fail on every other code. See [debugging](debugging.md) and the existing [PowerShell pitfall](../../../../docs/agent-context/pitfalls/windows-shell-fail-fast.md). |
| `nested-worktree-enametoolong`  | `ENAMETOOLONG`, deep `node_modules`, candidate inside another candidate/worktree                                 | Stop; use only the fixed short managed release path and the safety checker. See [worktree/bootstrap](worktree-and-bootstrap.md).                                                                                    |
| `runtime-evidence-dirty-tree`   | Temporary manifest, report, screenshot, or runtime evidence appears in `git status`                              | Stop before staging; move future evidence only to a prechecked ignored root runtime path and preserve the unexpected file for ownership review. See [testing/evidence](testing-and-evidence.md).                    |
| `patch-escape-newline-mismatch` | Repeated patch “invalid context”, quote escaping mismatch, CRLF/LF mismatch                                      | Reread exact current bytes and apply a smaller `apply_patch`; do not repeat blind text replacement. See [debugging](debugging.md).                                                                                  |
| `ssh-banner-tun-timeout`        | SSH banner exchange/timeout appears intermittently under VPN/TUN                                                 | Verify local route first, then at most one bounded retry using ignored local operator guidance. See [production](production.md).                                                                                    |
| `production-domain-tun-19818`   | Formal domain resolves to `198.18.x.x` on the local machine                                                      | Treat it as TUN synthesis, not the production address or successful verification. See [production](production.md).                                                                                                  |
| `remote-stdin-consumed`         | A remote script streamed on stdin stops after `docker compose run` or a database child                           | Do not resend blindly; use a reviewed remote script file/fixed control or detach child stdin. See [production](production.md).                                                                                      |
| `browser-smoke-empty-month`     | Browser smoke calls a valid empty schedule month a product failure                                               | Confirm month, fixture, API response, and assertion contract before changing product code. See [testing/evidence](testing-and-evidence.md).                                                                         |
| `diagnostics-off-still-bundled` | Release runtime hides diagnostics but package-size/source audit still finds diagnostic code                      | Runtime gating and bundle exclusion are different claims; inspect source/output. See [testing/evidence](testing-and-evidence.md).                                                                                   |
| `comparison-environment-drift`  | Test counts or package sizes differ across incomparable runs                                                     | Recreate clean parent/new runs with identical toolchain, command, profile, env, and fingerprint; otherwise do not compute a delta. See [testing/evidence](testing-and-evidence.md).                                 |
| `main-worktree-mixed-dist`      | Candidate passes only after main worktree built, or output SHA/source SHA disagree                               | Reject the evidence and rebuild producer outputs inside the clean candidate. See [worktree/bootstrap](worktree-and-bootstrap.md).                                                                                   |
| `miniprogram-version-local`     | Upload log, build profile, or UI reports `version=local`                                                         | Stop before upload; rebuild the exact SHA with an explicit semantic version and rerun the upload preflight. See [Mini Program](miniprogram.md).                                                                     |
| `repeated-1459-linking`         | Install repeatedly prints linking progress for 1459 packages with no dependency-input change                     | Stop reinstalling; verify the managed dependency fingerprint and reuse the existing worktree dependencies. See [worktree/bootstrap](worktree-and-bootstrap.md).                                                     |
| `duplicate-expensive-gates`     | Same final SHA and identical candidate fingerprint is sent through full gates again                              | Reuse intact recorded evidence; rerun only when a fingerprint/evidence/guard input changed. See [release candidate](release-candidate.md).                                                                          |
