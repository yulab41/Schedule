@import '../../styles/identity.wxss';
.guest-entry-copy { display: block; margin-bottom: 24px; color: var(--ui-color-text-secondary); font-size: var(--ui-font-size-sm); line-height: 1.5; }
.guest-entry-error { display: block; margin-top: 12px; color: var(--ui-color-danger); font-size: var(--ui-font-size-sm); }
.identity-card__button { margin-top: 18px; }
