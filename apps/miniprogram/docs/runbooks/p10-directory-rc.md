# P10 通讯录实体 Android RC 验收

本清单只验收微信体验版 `0.1.0-p9.20260826.15` 的原生通讯录，不打开生产 `organization` 能力，不提交审核或正式发布。所有号码和组织名称以当前体验版实际显示为准，不截图、不复制或外传真实号码。

## 进入与能力关闭

1. 使用已绑定的管理员或普通成员账号进入当前群组，在工作台 More 打开“院内通讯录”。访客账号不得显示该入口。
2. 当前生产能力预期为 `organization=false`：页面显示“通讯录暂未开放”，不出现加载中的真实号码，也不产生通讯录请求。
3. 返回工作台后再次进入，关闭态文案和导航不闪烁；切换到 320px 或系统大字号后仍可滚动、无横向溢出。

## 院内模式（能力开启的隔离环境）

1. 以组织能力开启的隔离环境复核：页面先显示 facets，再保持空态，只有输入关键词或选择筛选条件后才发起列表查询。
2. 依次选择院区、片区、楼宇、楼层、科室、单元、类型；每次选择都保留其他已选条件，结果摘要与列表同步更新。
3. 输入科室/姓名/号码关键词并确认搜索；清空关键词且无筛选时回到空态，不拉取全量目录。
4. 滚动到“加载更多”，确认 cursor 请求追加记录、不重复首屏、不丢失筛选条件。
5. 点击完整手机/座机号码，确认只触发一次拨号；只有内线号的联系人显示为只读文本，不触发拨号。

## 员工模式与权限

1. 切换“员工”标签，确认请求 employee-directory 路径，筛选标签变为“组织根、一级组织、二级组织、三级组织、四级组织、五级组织、类型”。
2. 普通成员与管理员分别复核可见条目差异；不得通过切换模式看到超出服务端权限的条目或号码。
3. 进入/退出页面、切换院内/员工和重复点击搜索后，不出现旧模式结果、旧号码或重复请求提示。

## 状态与尺寸矩阵

| 状态                  | 390 | 320 | 大字号 | 预期                            |
| --------------------- | --- | --- | ------ | ------------------------------- |
| ready/结果卡          | ✓   | ✓   | ✓      | 卡片可滚动，号码层级清晰        |
| loading facets/list   | ✓   | ✓   | ✓      | 只显示状态，不显示旧号码        |
| empty                 | ✓   | ✓   | ✓      | 有明确下一步，不拉全量          |
| error/retry           | ✓   | ✓   | ✓      | 重试可恢复，失败不保存输入/号码 |
| organization disabled | ✓   | ✓   | ✓      | 关闭态、无请求、无真实目录内容  |

## 通过条件

用户回复“P10 通讯录 RC 通过”后，才允许评估开启 `organization`；若任一项失败，请提供模式、尺寸、状态和现象。没有实体 Android 通过证据时，P10 不得标记阶段完成，也不得提审或正式发布。
