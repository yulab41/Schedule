.list-day-label {
  font-size: 30rpx;
  font-weight: 600;
}

.list-day-label.is-today {
  color: var(--color-primary);
}

.list-day-today {
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: var(--color-today);
  font-size: 22rpx;
}
